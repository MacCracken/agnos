---
name: AGNOS Kernel State
description: Live state only — kernel head, open cut, next bite, pins, flash gate, pointers.
type: state
---

# AGNOS Kernel — Live State

> **⛔ 120-LINE HARD CAP. NOT A LOG.** This file reached **694 lines** by absorbing arc narrative that belongs in [`CHANGELOG.md`](../../CHANGELOG.md), [`planning/gpu.md`](planning/gpu.md), or the burn ledger. Over 120 lines means consolidate: cut prose, never facts. Verify any single item against the live `VERSION` / `cyrius.cyml` before acting. **Last refresh** 2026-08-05.

## TRUE — measured, current

| Field | Value | Source |
|---|---|---|
| Kernel head | **1.56.40 — OPEN** (opened 2026-08-05, the local-IPC channel band `chan_*` on `#97`; bites 0-2 landed) | [`VERSION`](../../VERSION) |
| Previous cut | **1.56.39 RELEASED 2026-08-05** — `spawn_path #43` fd-redirect · ELF user-image floor `0x400000` · `pmm_kva_for_access` direct-map-always. 1.56.38 before it: BURNED and PASSED — latch auto-release at clean shutdown (boot 2 came up native with nothing typed) + a native, hardware-panned boot console. `Timer ticks before sched` **28** (800x600) → **149** (native, software scroll) → **11** (native + pan): native and the pan are ONE change, either alone is a regression | [`CHANGELOG.md`](../../CHANGELOG.md) |
| `build/agnos` on disk | 1,952,640 B, 2026-08-05 — **1.56.39 bare, NOT burned** (test build; the last flashed artifact was 1.56.38 at 1,951,688 B, burn-tag `b830c4d6…`). ⚠ Any smoke/test run rebuilds it without the burn flags — re-run `burn-prep.sh` before any flash | `scripts/burn/burn-verify.sh` |
| Cyrius pin | **6.4.78** | `cyrius.cyml [package].cyrius` |
| Bootloader | gnoboot **0.6.1** (GOP mode selection — obtains a real 2560x1440 framebuffer); Path C, `RDI = &boot_info`, magic `0x41474E4F`, entry `0x1000a8` | `gnoboot/VERSION` |
| Iron target | archaemenid — Beelink SER NUC, AMD Cezanne APU, **8c/16t (agnos parks APIC id ≥ 4, runs 4)**, 64 GB. Build host **is** the target, so no serial channel exists. | — |

⭐ **RESOLVED 1.56.35 — the `-smp 4` fault was `EFER.NXE` never enabled on the APs** (`smp.cyr:514`; the AP trampoline set LME only, so bit 63 of a paging entry was RESERVED and `proc_map_page_nx` sets it on every W^X data page and user stack). `-smp 4` reaches **connected 2 / presented 2, exit 95**.
⭐⭐ **BURNED 2026-08-03 — PASS: the desktop hosts two real client windows on iron at 4 CPUs** —
`present_probe` + crab's dual-pane file manager, 278 frames, keys delivered, clean Esc quit. A cross-CPU TLB shootdown IPI (vector 0xF0) landed alongside and rode clean. → `#tracker-15635-desktop-smp`.
⚠ **`AE_CLIENTS_SMP` defaults to `"1"`** — every desktop QEMU proof before 2026-08-03 was silently single-CPU. **Pass `AE_CLIENTS_SMP=4`**; it passes, and it is what archaemenid runs.
⚠ **The syscall-kstack restore (1.56.34-era) STANDS, and so does its 2-client result** — a 2026-08-03 retraction of it was itself wrong and is reversed. `aethersafha-clients-test.py` byte-scans the kernel and hard-exits on any selftest hook, and attaches a real NIC for DHCP, so that run was never rigged.
⛔ Do not over-retract: the operator's ruling is that TCP is the **wrong primitive**, not that it never worked — erasing the one un-rigged connection destroys the evidence for that distinction.

**1.56.35 is the desktop's kernel half** — opened 2026-08-02, scope in [`CHANGELOG.md`](../../CHANGELOG.md). The full rationale per item, and the substrate matrix that decides what each proof is worth, live in aethersafha [`planning/desktop.md` §6](https://github.com/MacCracken/aethersafha/blob/main/docs/development/planning/desktop.md).
⭐ **Six of its eight items are now DONE.** K1 (the SMP PDE fault) and K6 (`net_src_for` in the CHANGELOG) closed in the 1.56.35 cut itself; **K3 / K4 / K5 landed at 1.56.39**. Remaining: **K7** the local-IPC band and **K8** the PTY — both in 1.56.40, both pointed at [`planning/ipc.md`](planning/ipc.md) §9.

**LANDED 2026-08-02, QEMU-verified with a control, NOT yet burned — the syscall-kstack restore.** `execwait #37`'s step (h) restored this CPU's SYSCALL kernel stack to the **raw identity VA** `0xF10000 + cpu*0x10000` instead of its direct-map alias, while `syscall_kstack_reserve` and step (f) both use the direct map. Region 7's identity VA (14-16 MB) is inside the user-segment range, so **every completed foreground `run` re-armed the 1.51.x "ark won't run" fault** for any later proc whose image reached past 15.06 MB. `/bin/aethersafha` (14.87 MB) is the first binary large enough to hit it. Control: on the unfixed kernel the armed sequence **triple-faults the machine**; fixed, it reaches the point where the compositor stays up. ⭐ **The "2 clients connected + presented" half of this result STANDS.** (A 2026-08-03 retraction of it was itself wrong and has been reversed.) `aethersafha-clients-test.py` does **not** use the `AETHERSAFHA_SETU_SELFTEST` hook — it byte-scans `build/agnos` and **hard-exits** if the kernel carries one (`:85-91`, *"a kernel carrying any such hook does not fail this test, it INVALIDATES it"*), and it attaches a virtio NIC specifically so DHCP yields a real `net_ip` (`:129-134`). The run was on 1.56.34+, i.e. post-`net_src_for`. It is an honest, un-rigged result. ⛔ **Do not over-retract this.** The operator's ruling is that TCP is the **wrong primitive** for local display IPC — not that it never worked. Erasing the one un-rigged connection result destroys the evidence for that distinction. The **triple-fault-vs-no-triple-fault control** likewise stands. New harness mode `AE_CLIENTS_MODE=armed` exists precisely because every prior mode launched the compositor as the boot's FIRST command and could not see this. Paired userland change: agnoshi routes the foreground through `spawn_path #43` + a waitpid poll (pipelines and `>` stay on `#37`).

**HDMI audio is PARKED mid-bite, not abandoned** — the CRC null calibration (`MDO_OP_CRCCAL` / `#93` op `0x0A` / `/bin/modeset --crccal`) is still the correct next audio step and is still unbuilt. It goes first *within that arc* because `DONE=1, CRC=0` is equally "2048 zero samples traversed" and "the counter completed and nothing traversed", and the second **inverts which half of the pipe is at fault**; `--crccal` needs no ear, so it is immune to the sink-latched state that made ~24 prior burns un-adjudicable.

Landed for 1.56.34 but **never built, therefore unverified**: `MDO_OP_CRCCAL` + `mdo_crccal()` ·
`hda_hdmi_feed_running()` · `--crccal` + usage · 13 `kprint` length corrections. Required flags for any HDMI-audio calibration burn: `HDA_HDMI` + `HDMI_ATOM` + `GPU_AUDIO_PROBE` + `HDA_TONE`.

### Flash gate

▶ **The 2026-07-31 pause is RESOLVED** (user-directed, 2026-08-02). Today's kernel, harness and staged agnsh are one attributable change set: `check.sh` **23/23**, `sweep.sh` **15/15**. ⚠ The hazard recurs the instant two sessions share this tree again — it is a **worktree** problem, not a one-time event.

⛔ **`build/agnos` is only flashable straight out of `scripts/burn/burn-prep.sh`.** Prep deletes the artifact + stamp up front so an abort leaves NOTHING to flash, and any smoke run afterwards rebuilds it as a compile-gated kernel — that is how a DOOM_SELFTEST kernel got flashed on 2026-08-02. `burn-verify.sh` cannot catch a stale-but-self-consistent pair; absence is the only reliable failure mode.

⚠ The **desktop** burn wants a BARE production kernel — no `BURN_SELFTESTS`, no HDMI-audio flag set. The 1.56.34 **HDMI-audio** burn card is a different artifact: `build/agnos` **1,985,728 B** + `/bin/modeset` **41,800 B** (md5 `c16a0f3b…`), and it requires `HDA_HDMI` + `HDMI_ATOM` + `GPU_AUDIO_PROBE` + `HDA_TONE`. Do not mix the two — a string check cannot tell a bare kernel from an `HDMI_ATOM` one, only the size can. For modeset: `exit 95` means the writes went out; it does **not** mean sound came out.

`/.modeset-armed` is an arm-once-per-boot latch that survives reboots **on purpose**, so a wedging modeset cannot re-fire. A latch left by a prior boot blocks every arm of the next one (`exit 98`). Recovery is `rm /.modeset-armed` plus a power cycle — **no reflash is ever needed** for any modeset failure mode.

### Toolchain pin — provenance, not enforcement

`scripts/build.sh` invokes the cyrius wrapper as `"$CYRB" build --no-deps …` — **without `--strict-pin`**. A newer installed `cycc` therefore builds agnos **warn-only** until the pin is bumped, so check `cyrius --version` when provenance matters. For the freestanding kernel the pin is largely **provenance**: the binary has been `cmp`-byte-identical across pin moves (6.3.9 → 6.3.43 both produced `build/agnos` at 1,345,512 B, cmp-proven). Ring-3 consumers, not the kernel binary, are what a pin bump unblocks. Hard floor: `symlink` #63 (agnos 1.51.0) needs `sys_symlink`, which exists only in cyrius ≥ 6.3.6. Cyrius is hands-off; pin lag is often a deliberate hold, not drift.

### CORRECTED — this file previously carried both of these wrong

**`boot_info` is 120 bytes / `0x78`, not 80** (80 was the pre-0.1.0 v1 size). magic `0x41474E4F` @0x00 · version 2 @0x04 · struct_size **120 (0x78)** @0x08 · flags @0x0C · fb_phys @0x48 · fb_pitch @0x50 · fb_width @0x54 · fb_height @0x58 · fb_pixel_format @0x5C · fb_size @0x68 · END @0x70. Framebuffer fields are **inlined at fixed offsets**, not in the tag stream — the boot-shim canary reads `fb_phys` from raw asm at instruction #1.

**The AMD-Zen scanout residue was RESOLVED at 1.55.28** — a surface/raster **scale** mismatch, not tiling and not DCC; fix is `gpu_scanout_matchgeom` (read the real HUBP viewport + pitch, override
`fb_console`; read-only, no register writes). Its last residual — the ~84 lines painted before the aperture maps — is itself closed by 1.56.37's deferred paint. Detail → `planning/gpu.md`.

### Sweep timing — ✅ FIXED 2026-08-05: ~20 min → **395 s**, 16/16

The 2026-07-23 diagnosis was right and sat unfixed: **QEMU never exits on its own**, so every smoke burned its whole `QEMU_TIMEOUT` waiting for nothing. New `scripts/smoke/lib/qemu-dwell.sh` backgrounds QEMU, polls for a marker, then SIGTERMs **and waits for the process to exit** — which is what preserves the "log is complete once QEMU has exited" guarantee `hda-smoke` documents; a bare kill would reintroduce that flush race. The budget is unchanged and still backstops a hung boot. **35 smokes converted.** ⛔ Marker choice is the hazard, and it is structural here, not per-file luck: no sweep gate feeds stdin, so all are boot-phase-only and the shell prompt provably follows anything a selftest printed. A smoke that DRIVES the shell must pass its own last line instead.
⚠ Still on fixed dwells: `hda-smoke` / `hda-dual-smoke` use `-serial file:` (QEMU owns the file) rather than `-serial stdio`, which the helper does not yet cover.
⛔ **Do NOT "fix" a slow gate by shortening its timeout** — that trades dead air for flaky truncation.

### 1.56.40 — the channel band, bites 0-2 landed

**4** `#97 chan_op` COMPLETE — 2 MB region (reserved after `pmm_setup_directmap`; reached via `DIRECTMAP_BASE`, the only alias every per-proc CR3 mirrors) + CAPS/MINT/SEND/RECV/CLOSE, mask `0x1F`. Cursors live in KERNEL arrays, so ring 3 cannot forge occupancy, rewind a peer, or inject. Drop-oldest, counted. `chan_release_pid` at both death sites — missing from the first cut, and invisible because the boot selftest closes by hand. ⭐⭐ **BOTH §9.9 KILL CRITERIA CLOSED** by `chan-ring3-smoke` (in sweep): the region resolves from a RING-3 CR3, and an INHERITED fd — kernel mints and queues a record, then execs; `elf_load_from_file` copies the whole fd table, there is no CLOEXEC — refuses BOTH directions while the same proc's own channel works. ⛔ Mutation-proven only on the SECOND attempt: the owner and proc-epoch checks are REDUNDANT, so removing one proves nothing; removing BOTH makes the inherited fd accept records. An empty queue also confounded RECV until the kernel queued a record first. **5** ENDOWMENT — `CH_ENDOW` + `chan_place_into_child` in `proc_create_user`: a child is BORN holding a channel. Placement is a MOVE (owner + proc-epoch re-stamped to the child, so the parent's fd goes inert by the same rule as an inherited one); you can only endow what you own; refuses if the child is on the GLOBAL table (`vfs_fd_inherit` returns success on kmalloc failure). Arm is stored as endpoint+1 so zero-init means UNARMED — a −1 sentinel would have placed a channel nobody endowed into the first proc booted. ⭐ **No-op path proven FIRST** (hook present, `chan_endow_enabled = 0`, sweep 17/17) as §9.6 requires for the highest-risk bite; setting it back to 0 is the rollback. Proven in `chanx`: inherited fd inert AND endowed fd works in the SAME proc. ✅ **Announcement built**: `CH_ENDOW` returns the fd the child will hold and the PARENT announces it (Wayland's `WAYLAND_SOCKET` shape) — the kernel cannot, because `elf_load_from_file` bakes env at `elf.cyr:417-451` and creates the proc at `:473`. Placement uses the arm-time number and REFUSES if it is no longer free rather than re-scanning to a different slot than the one announced. Proven: parent announced fd 7, child used 7 directly. **6** ⭐⭐ **CUTOVER — setu's agnos client NO LONGER DIALS.** `setu_connect`'s agnos arm is four lines (floor check · read `AGNOS_CHAN` · return the fd); the TCP arm is DELETED, not gated (Linux keeps its own — a different target, not a fallback). `chan_send`/`chan_recv` replace `sys_write`/`sys_read`, so WOULD_BLOCK is finally an immediate, distinct answer instead of the stdlib's 30 s deadline returning 0 for both EOF and timeout. Floor ENFORCED via `CH_CAPS` (§9.9's last criterion). **setu 0.8.0** (2026-08-06). Pins cyrius **6.5.8**; declares a hard floor of agnos **1.56.40** — a client built from it REFUSES on a pre-`#97` kernel rather than appearing to work. ⚠ Not runtime-proven end-to-end — nothing sets `AGNOS_CHAN` until bite 7 (aethersafha). Verified: builds `--agnos`, Linux unchanged, no socket call in the agnos arm. **7** ⚠ **PARTLY LANDED, blocked on a setu FRAMING fix.** ✅ The listener is GONE on agnos; the compositor mints/endows/spawns placed (`placed: 2` measured) and polls minted channels; setu 0.8.1 + crab + aethersafha all repinned onto cyrius 6.5.9. ⛔ `presented: 0` — `setu_read_msg` reads a 16-byte header THEN a body, which cannot work on a record transport where one record IS one message. Filed: setu `issues/2026-08-06-read-msg-is-stream-shaped-on-a-record-transport.md`. ⭐ **The kernel half is fixed**: `CH_RECV` used to TRUNCATE a record that did not fit and consume the rest — stream semantics inside a record channel — so the header read ate the whole message and the failure surfaced two layers away in aethersafha. It now returns `-CH_E_ARG` and leaves the record queued. ⚠ Three of my own instruments lied on the way: a stale `puka_agnos` staged (stage-tools uses the ROOTFS name, not the source name — use `--build`), em-dashes truncating diagnostics on an 8-bit console, and the harness grepping for marker strings the cutover deleted (`launched: False` on a boot where both clients were placed). **3** `tests/chan/chantest.cyr`**3** `tests/chan/chantest.cyr` — the RECORD/BATCH/LIVENESS contract executed against Linux `socketpair(SOCK_SEQPACKET)` (18 assertions, in `check.sh`), so the kernel band has something EXTERNAL to be measured against; negative control: over `SOCK_STREAM` it fails exactly the 6 framing assertions. ⛔ Silent on the authority model — no Linux analogue; that is bite 5's kill criterion. **0** `epoll_wait`'s `found == 0` path ran a bare `arch_wait()` (`hlt`, no `sti`, inside an IF=0 handler) — an `epoll_wait` on an unexpired timerfd **hung the box**. Deleted; it polls like every other agnos not-ready answer. **1** shm gets `shm_owner`/`shm_epoch` + `shm_release_pid` at both death sites;
`#74` gated on owner, `#72`/`#73` **warn-counted and still allowed** (a cross-owner read is what the compositor does every frame). **2** `proc_epoch[16]`, bumped in `proc_alloc_slot`, written and never read. All three carry selftest assertions with negative controls.
⛔ `SYSCALL_HARDEN_SELFTEST` had **no runner since 1.41.5** and had stopped compiling (3 args to 4-arity
`ksyscall`); it is the only epoll/timerfd/signalfd coverage there is. Now `syscall-harden-smoke.sh`.
⛔ **`shm_release_pid` is INERT in the desktop workload** — instrumented, frees nothing across a full
`desktop` run. The selftest is its coverage; the desktop is not.
⛔ **`AE_CLIENTS_MODE=desktop` is FLAKY** — 3 runs on one kernel gave two opposite failure shapes, and a flake was briefly misattributed to bite 1 (a disable-it bisect "confirmed" it; instrumentation showed the function never runs). ⭐ The framebuffer gate was right in the run where serial was wrong. Do not gate on `desktop` mode until this is understood.

## FALSIFIED — do not re-open

- **Sequencing is NOT eliminated.** The M9 burn (1.56.15) that "eliminated" it was a **null experiment** — both arms streamed digital silence (no `HDA_TONE`, no ring-3 feed), so a healthy, entirely believable log was produced by a zero-filled ring. Retracted; sequencing is re-open.
- **L1 (Linux-userspace discriminator) returned VOID**, 2026-07-24/25 — positive-control arm D was silent with all four validity conditions verified, so it carries **zero** information. Never write it up as "sequencing exonerated". Structural cause: the two writes agnos performs routinely (`OTG_MASTER_EN` → 0, and the `FE_SOURCE_SELECT` BE↔FE bracket) each hard-wedge the APU from userspace.
- **The GOP-side `SetMode` lever as a way to move the SCANOUT** — same-mode re-arm (gnoboot 0.4.1, Attempt 74) and different-mode bounce (gnoboot 0.4.2, Attempt 78). Neither produced flicker on VGA or HDMI. ⛔ **CORRECTED 2026-08-03 — the reason is not "AMD Zen UEFI elides `SetMode`".** That sentence stood here and was wrong: gnoboot 0.6.1's `SetMode` demonstrably **worked**, and the firmware allocated a real 2560×1440 framebuffer (`fb: … phys=0xd0000000 size=0xe10000`). What `SetMode` does not do is touch DCN — it is **GOP-scoped**, which is all UEFI ever promised. The earlier "elided" reading came from a burn where the `fb:` line was serial-only and this box has no serial, so the GOP side was invisible. **What survives: no fourth `SetMode` variant — the lever is finished.** Native resolution is a kernel-side DCN write (`MDO_OP_NATIVE`), not a bootloader trick.
- **The tiled/DCC scanout thesis** (OSDev #57150) — survived two months as the last standing hypothesis and was still wrong. See the correction above.
- **Path A (ELF64 + multiboot2 via GRUB on UEFI)** — dead 2026-05-13; GRUB's `grub_relocator64_efi_boot` self-patches its own `.text` under strict W^X. gnoboot replaced it. Do not revive by patching GRUB.
- The HDMI-audio dead-end corpus (DCCG symbol-clock lead · the register-poke class · sample magnitude · AFMT RAMP · the offset axis, 105/105 · the DMCUB T-series · `phyid = 0`) lives in [`planning/gpu.md`](planning/gpu.md). **Surviving candidates: (b) a write that does not latch · (c) the bare-metal environment.**

## OPEN

- **1.56.39 THREE KERNEL ITEMS THE DESKTOP WAS OWED — built, QEMU-green, not burned.** `spawn_path #43` now consumes an armed `exec_redirect #62` into the child's **private** fd table (apply-only — `#43` has no "afterwards" to restore at, unlike run-to-completion `#37`), refusing when `proc_fd_base[pid] == 0` because that is the **global** table and a redirect written there is unrecoverable. ELF user-image floor `0x200000` → `0x400000` at **all four** sites (both loaders' `e_entry` + `PT_LOAD`), keeping a crafted `p_vaddr` off PD[1] where the boot TSS RSP0 seeds live; measured free — all **44** staged binaries already base at `0x400000` / `e_entry 0x4000b0`. `pmm_kva_for_access` returns `DIRECTMAP_BASE + phys` **unconditionally** — the old 256 MB split handed back an identity VA that means something else under a per-proc CR3, which `hda.cyr` already routed around by hand. ⛔ **Ordering is now load-bearing**: the direct map installs at `main.cyr:232`, earliest caller is `hda_probe` at `:661` — do not add a caller ahead of 232. ⭐ **The `#43` redirect ships with a boot selftest** (`spawn_redirect_selftest`, under the existing `EXEC_REDIRECT_SELFTEST` gate) because nothing arms `#62` before a `#43` yet, so the apply path had no caller. It earned its keep immediately: the first build guarded on `proc_fd_base_get(pid) == 0` and the negative control caught that proc 0's base is set **explicitly** to `&vfs_table` (`vfs.cyr:264`), so a `== 0` test would have written the GLOBAL fd table. Guard is now `vfs_fd_table_of(pid) == &vfs_table`. ⭐ **The clients harness was reporting on launch paths it never ran** and is fixed in the same cut: one mode runs per boot, so an unrun arm's `None` exit code was read as a failure — `MODE=bg` printed *"FOREGROUND does not [work]"* **and invented a cause for it**, `MODE=fg` printed *"both launch paths"* from one path. A false red and a false green from one block. It now tracks which arms actually ran, at the launch sites. ⭐ **`desktop` mode also gates on the FRAMEBUFFER now**, not on the compositor's own serial claim about itself — signal 3,500 px vs a console null of 0, floor 200, and a negative control (floor above signal ⇒ **exit 1**) proves the gate is wired to the exit code. dim-green is excluded on purpose: at 952,731 px it is chrome, not a client border, and gating on it would pass a desktop hosting nothing. Verified: `check.sh` **23/23**, `sweep.sh` **15/15**, `exec-redirect-smoke` **4/4**, and `aethersafha-clients-test.py` at **`AE_CLIENTS_SMP=4`** reaches **connected 2 / presented 2 / exit 95 on BOTH launch paths** (`fg` and `bg` run separately).
- **1.56.36 / 1.56.37 / 1.56.38 — RELEASED, BURNED, PASSED.** The boot-console half of the desktop arc is CLOSED on iron: native 2560x1440 with the scaler bypassed, a hardware-panned console, no banded first frame, and a modeset latch that releases itself at clean shutdown. `Timer ticks before sched` **28** (800x600) → **149** (native, software scroll) → **11** (native + pan) ⇒ native and the pan are ONE change. ⛔ **A self-imposed constraint in a comment is the hardest kind to see** — *"IT HAS TO BE HERE, NOT EARLIER"* was a fact about where someone put a function call, not about the hardware; the modeset was waiting ~2 s on a refresh measurement for a number the same function produced in microseconds. Per-cut detail → CHANGELOG. ⚠ Not taken: the modeset still cannot start before ~log line 115 (`modeset_arm` needs the ext2 latch), so 116-137 still paint at 800x600; removing that moves `gpu_display_probe()` ahead of `gpu_fw_load()` — a GPU boot-order change **QEMU cannot exercise**.
- ⭐ **CLOSED 2026-08-05 — the aethersafha "a little slow" carry-forward.** Same burn as the native modeset, and that WAS the diagnosis: 800x600 → 2560x1440 is **7.68x the pixels**. Cause was per-pixel overhead, not memory — `fill_rect` measured **12.72 ns/px** vs the clear's **1.03 ns/px** for identical stores (a call, four bounds compares and seven header `load64`s per pixel). Clamp-once row-walking (`fill_rect` + new `blit_rect`): chrome **23.45 → 2.57 ms**, CPU surface copy **23.97 → 4.18 ms**, **GPU frame 27.24 → 6.40 ms (4.3x)**; agnos framebuffer pixel counts IDENTICAL before and after. ⚠ The full-screen clear is now **60% of a frame** — that is the damage model's job (`AE-0a`), not a faster loop.
- **1.56.34 HDMI audio** — PARKED by operator decision 2026-07-31. Do not re-open without an explicit ask.
- **Order once the tree is clear**: build → `check.sh` → `sweep.sh` → the in-boot A/B listening harness. That harness needs a **negative control in the same boot**, and its arms must be blinded by **tone band**, never by removing the tone (arms ~an octave and a half apart: LOW for `--audio-pre`, HIGH for `--audio-post`, so the operator reports *which*, not yes/no, and a null is real data). All audio arms run in ONE boot — a cross-boot comparison cannot control the sink's latched amp/mute state.
- **`modeset-tool-smoke` mask — CLOSED in-tree, and moved again at 1.56.36.** `MDO_OP_SUPPORTED` is now **`0xE7F` = 3711** (un-armed) and **`0xFFF` = 4095** (armed) — bit 11 = `MDO_OP_NATIVE`, added to BOTH values because it carries no audio content and must exist in a plain kernel. `scripts/smoke/modeset-tool-smoke.sh` asserts 3711/4095 in both directions. History: 127/511 → 639/1023 (PIXCLK, bit 9) → 1663/2047 (CRCCAL, bit 10) → 3711/4095 (NATIVE, bit 11).
- **The STAGE-1 imitation-edge removal** is a restructure, not a deletion — that block also holds the AVMUTE unmute and the FIFO drain arming. It follows the instrument work, with a burn, never blind.
- **SDMA** — parked with a known resume state, an agnos **setup gap** and not a hardware limit (Linux drives SDMA on this box). Resume state: `rptr 0 wptr 44 cnt 266269 base 4102029824 halt 0 cap 0 cntl 262146`. Detail in [`planning/gpu.md`](planning/gpu.md).
- **aarch64** — compile-only, no boot harness, **not gated** (test.sh/check.sh/CI are x86-only). Its own future arc (decade 1.6x), not maintained in lockstep.

## Pointers — the content lives at these, not here

- **GPU / display / HDMI audio** — every register, burn result and falsified hypothesis: [`planning/gpu.md`](planning/gpu.md). *The* single GPU document; no new GPU arc docs.
- **Burns** — agnosticos [`iron-log.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-log.md), the consolidated burn ledger (⚠ being created by the 2026-08-01 doc consolidation; until it lands the burns are in `iron-nuc-zen-log.md` + `-mvp.md` + `-mvp2.md`). Measured captures in agnosticos `docs/development/prior-art/` — those `.txt` files are irreplaceable iron evidence, reference them, never edit them.
- **Syscall ABI**, per-call contract — [`agnos-userland-abi.md`](agnos-userland-abi.md). **Build flags** and compile gates — [`build.md`](build.md).
- **Per-cut narrative and size trajectory** — [`CHANGELOG.md`](../../CHANGELOG.md). **Milestones** — [`roadmap.md`](roadmap.md). **Ecosystem-wide state** — agnosticos `state.md`.

`scripts/version-bump.sh` refreshes the header date and the Version row atomically with the bump; the body is a manual sweep at each closeout. If a section here starts telling a story, it belongs in one of the pointers above.
