---
name: AGNOS Kernel State
description: Live state of the AGNOS kernel — version, sizes, sibling pins, subsystem rollup, in-flight slots. Refreshed every release.
type: state
---

# AGNOS — Live State

> **Last refresh**: 2026-06-04. **1.41.1 — shell-separation arc (OPEN): `agnsh` (agnoshi) becomes the interactive shell exec'd from disk; the in-kernel `shell()` shrinks to an emergency fallback.** 1.41.0 was the **cycle-open** (audit + staging, no kernel code); **1.41.1 landed the first kernel bite — blocking ring-3 keyboard `read(fd=0)`**. The boundary audit (agnosticos [`shell-separation-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/shell-separation-prior-art.md) — all 35 verbs classified + syscall gap analysis) + `scripts/stage-agnsh.sh` (stages `agnsh` → `build/rootfs/bin/agnsh`). **Gating prerequisite was cyrius-side: `CYRIUS_TARGET_AGNOS` — ✅ RESOLVED at cyrius 6.0.55/6.0.56** (see the 1.41.4 entry below). `agnsh` is OS-agnostic (zsh/bash-class) and was building against the **Linux** syscall ABI (the only Cyrius target then available to build against), so it couldn't run on AGNOS's sovereign syscall surface until Cyrius gained an agnos target peer. The port is "add a Cyrius target," not "agnos answers Linux syscalls" (sovereign-surface invariant holds). Agnos-side bites: **1.41.1 LANDED — blocking ring-3 keyboard `read(fd=0)`** (`kbd_read_blocking` in `syscall.cyr`; RAW per ABI O1; cooperative `hid_poll` drain so it works with IF masked — no preemption dependency; busy-spin while idle is a power follow-on; build 1,050,824 B; sweep no-regression — read(0) dormant in all smokes, iron-burn-pending with agnsh). The `read(fd=0)` row is now 🔒 FROZEN in `agnos-userland-abi.md`. **1.41.2 LANDED — boot_info consumers (parallel side-bite, NOT shell-separation): canary + ACPI RSDP fallback** (the agnos half of gnoboot v0.5.0; `core/main.cyr` canary reports `initramfs`/`cmdline`/`acpi_rsdp` from `boot_info_copy`, `acpi_init` falls back to `boot_info+0x38` when the legacy EBDA/ROM scan finds nothing — which it does under UEFI; the fallback FIRED in the OVMF exec-smoke: `ACPI: RSDP at … (boot_info)`, RSDP at ~531 MB above the legacy range; `sweep.sh` 7/7 no-regression; aarch64 untouched — both files are `#ifdef ARCH_X86_64`). Taking 1.41.2 shifted the shell-separation ladder down one. **1.41.3 LANDED — shell-separation bite 2: real FS syscalls** (9 syscalls on the mount-routed VFS against the frozen ABI: `open`(7) re-route + AO_* flags, real `mkdir`(9)/`rmdir`(10)/`sync`(12), new `getdents`(29)/`unlink`(30)/`rename`(31)/`link`(32)/`stat`(33), plus the **`a4=r10`** ABI extension — O2 — for the 4th arg, threaded via one `mov r9,r10` at the syscall-stub entry into a `ksyscall_a4` global; `ksyscall`'s arity + in-kernel callers unchanged. `chdir`/`getcwd` are NOT syscalls — CWD is userland-owned per ABI §3.2. getdents emits agnos reclen records via a new `VFS_EXT2_DIR` fd + `ext2_getdents`; stat fills the 48-byte struct via `ext2_fill_stat`; ext2 path-split for the syscall layer is a new `vfs_ext2_parent`. Validated by `FS_SYSCALL_SELFTEST` driving all 9 through `ksyscall` with a `pmm_alloc_2mb` user-range scratch + ext2 `path_lookup` side-effect checks → `fssys: ALL PASS`; `sweep.sh` 7/7 — the stub change runs on every syscall and the exec-from-disk ring-3 gate stays green). **1.41.4 RELEASED — shell-separation bite 3: first boot-to-agnsh-on-disk + cyrius 6.0.56 toolchain leap (pin `6.0.14` → `6.0.56`).** The cyrius-side `CYRIUS_TARGET_AGNOS` peer that had gated this whole arc **LANDED** (cyrius 6.0.55 — `lib/args_agnos.cyr` + an init-stack capture closing the `args_init()` `#UD`, the `lib/io.cyr` agnos file-op ABI fix, a `sys_chmod` no-op; cyrius 6.0.56 — `lib/process_agnos.cyr`, the `sys_spawn`-based `exec`, + `W*` macros + chrono/`exit` normalization). So PID 1 (`kybernet`) now `exec`s the real `/bin/agnsh` (the `cyrius build --agnos` agnoshi binary, 282,880 B) in ring 3 off the ext2 root via new `kybernet_exec_agnsh()` (`elf_load_from_file` → `exec_and_wait` → `proc_reap` → exit-code; `init.cyr`); the in-kernel `shell()` is now a **fallback only** (fires on load failure). **Validated end-to-end in QEMU** by new `scripts/agnsh-smoke.sh`: production kernel + ext2 image seeded with `/bin/agnsh` → boot reaches the **agnsh prompt** in ring 3 (`agnoshi 1.3.x / AI-native shell…`), **no `#UD`, no emergency-shell fallback — PASS ×3, deterministic**. The agnsh `--agnos` build is now `ud2`-free (0 unresolved-call sentinels). Kernel rebuilds clean on 6.0.56 (multiboot2 ELF64, entry `0x1000a8`, **1,061,944 B** — freestanding `klib/`, so the pin is a `cycc` codegen bump only). **Iron burn pending** (boot-to-agnsh's first hardware touch). **1.41.5 RELEASED — syscall-ingress hardening / security sweep.** A 6-dimension multi-agent audit (adversarially verified, 0 refuted; the high cluster cross-converged from two independent dimensions) of the now-live ring-3→ring-0 surface (1.41.x put a real userland shell across the syscall boundary; 1.41.3 added 9 FS syscalls taking user pointers) surfaced 26 findings; **15 fixed** (all 10 high + 2 medium + 3 low), all behavior-preserving. Headline: **fd type-confusion** — `epoll_ctl`/`epoll_wait`/`timerfd_settime` never checked `ktag`, so a ring-3 caller could pass a foreign fd whose `payload[2]` is a kernel heap ptr / an ext2 fd's inode# and turn the handler's `store64`/`load64` into an **arbitrary kernel write/read** (closed with `VFS_*` tag checks mirroring `getdents` + a watched-fd bound vs `vfs_table` OOB). Plus a **1 GB user-VA ceiling** on `is_user_range`/`is_user_ptr` (was unbounded above 2 MB → could aim a kernel load/store at MMIO/high mem; every legit user addr <1 GB), the **ext2 basename overflow** of `ext2_dir_buf` (4 KB) capped at 255 in `vfs_ext2_parent` (one ingress for mkdir/create/rename-dst/link-dst), `spawn` ELF-buffer validation, ELF `p_vaddr` upper bound (no arena alias), `sys_mmap` length-overflow cap, FAT-chain cycle guard, `epoll_create` OOM, DNS name cap. Validated: `sweep.sh` **7/7**, `FS_SYSCALL_SELFTEST` **`fssys: ALL PASS`**, `agnsh-smoke` **PASS** (real ring-3 agnsh to prompt), `check.sh` 11/11; build 1,062,872 → **1,063,016 B**. Deferred to 1.41.7: #11 ELF-leak teardown (invasive in the iron-validated exec path), #16 `proc_reap` non-LIFO (non-triggering under run-to-completion), #21/#22/#23 refactors (#22/#23 not behavior-sound → own pure-refactor cut). Audit doc: agnosticos [`kernel-1415-hardening-audit.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/kernel-1415-hardening-audit.md). **1.41.6 RELEASED — refactor + exec-hardening (the deferred half of the 1.41.5 sweep).** 1.41.5 shipped the hardening but punted the *refactoring* + one medium exec fix; this cut finishes them as a dedicated, independently-validated pass. First a **safety net**: a new `SYSCALL_HARDEN_SELFTEST` (`shsys: ALL PASS`) that regression-locks every 1.41.5 security fix AND covers the previously-untested epoll/timerfd/signalfd region — so the dispatcher refactors run against a real test. Then: **#11 ELF-leak** — `elf_load`/`elf_load_from_file` now pre-pass-validate every PT_LOAD segment *before* `proc_create_address_space`, so a malformed ELF is rejected with zero allocations to leak (chosen over teardown in the iron-validated exec path; rare OOM-mid-mapping leak is a separate class); **#22 dedup** — a shared `sc_path_ok` path-arg preamble across the 7 FS syscalls + collapsing the identical-body `FS_FAT`/`FS_EXFAT` arms to `||`; **#21** — the trailing 1.41.3 FS group reordered into numeric order (29→33; verified every num 0-33 appears once). **#23/#14 canary** documented (non-uniform by design — ksyscall has no stack buffer; single-tail rejected as risky). All behavior-preserving — `sweep.sh` **7/7**, `fssys: ALL PASS`, `shsys: ALL PASS`, `agnsh-smoke` PASS, `check.sh` 11/11; build 1,063,016 → **1,063,944 B**. The deeper resolve+per-backend dedup stays NOT done (divergent per-syscall, not behavior-provable). **1.41.7 RELEASED — FAT/exFAT content-write via the syscall ABI.** Surfaced while planning the shrink: `vfs_write` (the `write(fd)` syscall) was ext2/pipe/device-only, so FAT/exFAT content-write was reachable ONLY from the in-kernel shell's `echo>` (`vfs_write_secondary`) — agnsh couldn't write a data partition and the FAT/exFAT write tests were shell-coupled. Landed first so the shrink doesn't strand the capability: new `VFS_SEC_WFILE` write-fd — `open(7)` with write-access (`AO_WRONLY`/`AO_RDWR`) on a FAT/exFAT mount returns a fd that buffers content + flushes whole-file via `vfs_write_on` (→ `fatfs_write_file`/`exfat_write_file`) on close. Backed by a **static 4-slot pool freed on close** (NOT `kmalloc` — the alloc-only heap leaks + exhausts; a per-fd 8 KB `kmalloc` failing late in the selftest is exactly what surfaced this). New `syswr:` selftests land real content on **both** FAT and exFAT disks (`SYSCALL-FAT-WR` / `SYSCALL-EXFAT-WR`), gated in fat-write-smoke + exfat-write-smoke. sweep **7/7**, `fssys`/`shsys` PASS, agnsh-smoke PASS, `check.sh` 11/11; build **1,083,176 B** (+~19 KB: the static pool). Limits (follow-ons): 4 KB whole-file cap/fd, no `AO_APPEND`/streaming, no freeing allocator. **1.41.8 RELEASED — decouple the FS-write test harness from the shell verbs (migrate half of the shell-shrink).** The FAT/exFAT/ext2 write SELFTESTS drove the shell's write verbs via `sh_exec("touch …")`, blocking their deletion. They now drive the write path via SYSCALLS — new `sc_touch`/`sc_echo`/`sc_mkdir`/`sc_rmdir` helpers (`ksyscall` open/write/mkdir/rmdir, `strlen`-computed lengths) — the same path agnsh uses. `rm`/`mv`/`sync`/`cat` stay shell verbs (recovery set); the exFAT `exfat_find` find-backs + `exfatw:` markers are unchanged. `sh_write_selftest` (redundant ext2 shell test) retired for a syscall block keeping `/shdir/keep.txt` + `/shtmp` checks valid. Same backend → identical on-disk files, so fat/exfat/ext2-write smokes pass **unchanged**; sweep **7/7**; build **1,084,312 B** (+~1 KB sc_* helpers). Planned by a multi-agent map + adversarial completeness spec (which caught — and I re-derived past — an off-by-one path length the spec text itself miscounted). The shell write verbs are now UNDRIVEN by any test. Next: **1.41.9 — delete the now-undriven non-recovery verbs** (`touch`/`echo>`/`mkdir`/`rmdir`/`ln`/`bench`/`ps`/`free`/`jbd2` + `bench_report` + dead `sh_write_selftest`) → recovery shell = `help`/`cd`/`pwd`/`ls`/`cat`/`run`/`mv`/`rm`/`sync`/`reboot` + non-write verbs, then **1.41.10 — arc-close + iron burn**. Emergency-shell triggers on **any** boot-to-agnsh failure + a future key-chord. **1.40.14 RELEASED — 1.40.x arc closeout: process teardown/reaping + hardening.** `proc_reap`/`proc_free_address_space` (`proc.cyr`) free an exited program's per-process pages (ELF + stack huge pages + the 3 PT pages, identified by the U/S user bit) and LIFO-reclaim its proc-table slot from `sh_cmd_run` — closing the leak that capped interactive `run` at ~3 invocations (`pmm_alloc_2mb` exhaustion: 6 usable 2 MB regions / 2 per run). Also removed the dead `u_pml4/u_pdpt/u_pd` KPTI allocation (orphaned since the 1.40.3 KPTI collapse — a silent 3-page/exec leak `proc_reap` couldn't reach) and made `vfs_resolve_mount`'s reloff deterministic on the `FS_NONE` path. exec-smoke **7/7** (two new reap-stability assertions green), `sweep.sh` **7/7**, `check.sh` 11/11; EXEC-selftest build **1,054,936 B**, production **1,050,408 B**. **Iron re-burn pending** (touches the exec/AS path) — rides the Track-B FAT/exFAT verbs burn. **1.40.13 RELEASED + IRON-VALIDATED — VFS mount-namespace routing** (`{prefix→backend}` table + longest-prefix resolver; FAT shell verbs reachable while ext2 owns `/`) + `fatfs_init` non-ESP preference. **The whole 1.40.x arc is now iron-validated** — the `14013_final*` burn (2026-05-31) confirmed exec-from-disk (1.40.9), the scheduler-reset fix (1.40.10), the boot-stack relocation (1.40.12), AND mount routing (1.40.13) in one boot: `/bin/prog2`+`/bin/argv` run in ring 3, `vfsw:` FAT verbs pass with ext2 at `/`, and the box rides clean through `Activating scheduler...` → DHCP `.157` lease → `Launching kybernet...` (no reset). The mount-namespace change also **collapses the FAT+exFAT iron verification from two burns to one** (both backends mount at once). **1.40.12 RELEASED — boot-stack memory-safety fix; `sweep.sh` now 7/7 (was 6/7).** The boot stack was based at `0x200000` (2 MB) and grows down; the 1.40.x kernel image **crossed 2 MB**, so `.rodata` string literals now sit just below `0x200000` and the stack was silently overwriting them (single RWE segment → rodata writable). Surfaced as the long-red exFAT-read `upcase-checksum` smoke row — but the exFAT read path was **correct all along** (the up-case `TableChecksum` matched); only its success-message literal was being stomped. Caught with a QEMU GDB watchpoint (writer = `push %rax` at `rsp=0x1ffbf8`). Fix: relocate the boot stack to `0x380000` (all three `boot_shim.cyr` sites) — PMM-reserved region 1, above the image top, below the `0x3F0000` syscall kstack. **Latent corruption affecting any rodata literal near 2 MB, not exFAT-specific** — a general memory-safety fix. Validated: `exfat-smoke.sh` PASS, **`sweep.sh` 7/7**, `check.sh` 11/11, `test.sh` 4/4; build **1,046,736 B** (byte-size unchanged — only shim immediates). **Iron re-burn recommended** (moves the boot-stack base; QEMU-green but a fundamental boot invariant where QEMU-vs-Zen can differ). **1.40.11 RELEASED — FAT/exFAT Track-B opener (test-harness only, no kernel change):** the 2026-05-31 "exfatprogs 1.3.2 mkfs format-drift" issue was **RESOLVED (misdiagnosed)** — no drift; the all-red failure was running the smoke against a leftover exec-iron `build/agnos` with no write self-test compiled in. Fix: a wrong-build guard in `exfat-write-smoke.sh` + sibling `fat-write-smoke.sh`/`ext2-write-smoke.sh`. **1.40.x exec-from-disk arc CLOSED (base-maturity SECOND leg — run a program loaded from the filesystem; the FIRST leg, FS-crash-safety, closed across 1.37/1.38/1.39).** **1.40.8 RELEASED — exec harden + burn-prep**: fixed an argv string-bytes bug (1.40.7 wrote the arg strings into a KERNEL scratch buffer while `argv[i]` pointed into the USER stack — `argc` at `[rsp]` was correct but `argv[i]` dereferenced garbage; now written to `stack_phys`, the user stack's phys alias) + an argv string-area overflow bound (`str_o+tl+1 < 0x4000` atop the `argc<=8` cap). Stronger gate: `/bin/argv Z` -> **exit 90** (argv[1][0]='Z'=0x5A) replaces the 1.40.7 argc-count run (deref subsumes count). New `scripts/burn-prep.sh` (one-command iron prep: sweep -> build EXEC kernel -> flash/watch steps). **Found (top exec follow-on)**: the 3rd sequential `run` in one boot fails at load — each exec leaks its 2 MB segment/stack pages (no process teardown), so `pmm_alloc_2mb` exhausts at exec #3; the selftest stays within the proven 2-exec envelope; **process teardown / page reclaim is the next exec bite** (it gates real interactive multi-program use). exec-smoke 7/7, sweep 6/6, `check.sh` 11/11, build **1,034,768 B**. **Iron burn READY (staged 2026-05-30, user-driven, PENDING)** — one hardware session, TWO tracks: **Track A** = exec-from-disk (`EXEC_SELFTEST` kernel via `install-usb.sh --update`; self-seeds `/bin/prog2`+`/notelf`+`/bin/argv`, dispositive bar `EXEC-DISK-OK`+`run: exit 42` on real Zen, + 1.40.8 `run: exit 90` argv deref); **Track B** = the 1.39.x VFS FAT/exFAT verbs (SEPARATE `FATFS_WRITE_SELFTEST`/`EXFAT_WRITE_SELFTEST` flashes on a non-ESP USB stick — folds the long-pending 1.34.x FAT/exFAT iron touch). Prep `scripts/burn-prep.sh`; rubric + outcomes → agnosticos [`iron-nuc-zen-log.md#tracker-139-cycle`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-nuc-zen-log.md#tracker-139-cycle) + step-by-step [`exec-iron-manual-tests.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/exec-iron-manual-tests.md). **1.40.0-1.40.7**: 1.40.0 opened the arc (streaming ELF64 loader `elf_load_from_file` — reads an ext2 path into the mapped process pages, reusing the hardened `elf_load` validation); 1.40.3 ring-3 execution (`exec_and_wait`/`enter_ring3`/`kernel_resume`: SYSCALL/SYSRET, iretq, SMAP STAC/CLAC, EFER.SCE) + exit-code capture; 1.40.4 ENOEXEC (non-ELF refused) + E2BIG (16 MB cap); 1.40.5 subdir/CWD path resolution + clean `exec_and_wait` return + the `scripts/sweep.sh` two-arc sweep + manual iron checklist; 1.40.6 multi-`run` in one boot (kernel_resume restores the boot CR3; `proc_current` tracks per-run exit) — validated at 2 execs; 1.40.7 argv passing (SysV init stack argc/argv/NULL/envp/auxv; `/bin/argc one two` -> exit 3). Ten ring-3 bring-up bugs resolved en route (all AGNOS-side, NOT Cyrius; core blocker: `mov cr3,r10` mis-encoded REX.R->cr11 #UD, fixed to REX.B). Arc plan: agnosticos [`exec-from-disk-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/exec-from-disk-prior-art.md). **1.39.9 RELEASED — VFS generic-write lift arc CLOSE (bite 9): FAT/exFAT subdir paths** (path-walk parent resolution `fatfs_resolve_parent`/`exfat_resolve_parent` + `*_in_dir` finders wired into open/create/write/delete/mkdir/rmdir/rename; `mv` bounded to same-parent). **1.39.8 RELEASED — VFS lift bite 8: mount-registry consolidation + hardening** (`vfs_secondary_select` non-ESP-preferring secondary selection; `vfs_sec_name_ok` name bounds; whole-file + offset read helpers). The 1.39.x VFS arc + 1.34.x FAT-family work make ext2 + FAT + exFAT fully path-addressable incl. subdirs. **1.39.7 RELEASED — VFS generic-write lift, bite 7: `mv` (rename) + `sync` on FAT/exFAT**: the last functional verb. `fatfs_rename` (in-place dirent name rewrite — size-independent, files+dirs), `exfat_rename` (re-emit dir-set at the SAME clusters + soft-delete old set; no content copy), `vfs_rename_secondary`/`vfs_sync_secondary` (sync = `blk_flush_on` the backend). `sh_cmd_mv`/`sh_cmd_sync` ext2-absent branches wired. **The full shell verb set now works on FAT + exFAT**: `cat`/`ls`/`touch`/`echo >`/`rm`/`mkdir`/`rmdir`/`mv`/`sync`. Validated: fat-write SHMVA→SHMVB present/gone + exfat-write in-kernel mv OK, **`fsck.fat`/`fsck.exfat -n` clean** (no cross-link/orphan from the exFAT re-emit); bites 1–6 no-regression; ext2 untouched. `check.sh` 11/11, build **1,008,816 B**. **Next (1.39.8 — arc close): mount-registry consolidation + FAT/exFAT subdir paths + hardening + iron burn.** **1.39.6 RELEASED — VFS generic-write lift, bite 6: exFAT `mkdir`/`rmdir`**: completes directory create/remove across both secondary FSes (FAT landed 1.39.5). New `exfat_mkdir` (alloc+zero a cluster — empty exFAT dirs have **no `.`/`..`** — then emit a Directory-attr dir-set with NoFatChain), `exfat_rmdir` (dir+empty check, reuse `exfat_delete` teardown), `exfat_zero_cluster`/`exfat_dir_is_empty`; `exfat_emit_set` gained a `fattr` param (callers pass `0x20`, mkdir `0x10`); `vfs_mkdir_secondary`/`vfs_rmdir_secondary` exFAT arm. Validated: exfat-write-smoke shell mkdir find-back + rmdir gone + **`fsck.exfat -n` clean** (Directory dir-set structurally valid); FAT 31/31 + bites 1–5 no-regression; ext2 untouched. `check.sh` 11/11, build **1,005,184 B**. **Next (1.39.7): `mv` (rename) + `sync` dispatch.** **1.39.5 RELEASED — VFS generic-write lift, bite 5: FAT `mkdir`/`rmdir` (new backend capability)**: the first bite that *adds* a filesystem capability, not just dispatch. New `fatfs_mkdir` (FAT32 — alloc cluster + init `.`/`..` + publish `0x10` dirent, crash-safe order), `fatfs_rmdir` (dir+empty check, reuse `fatfs_delete` teardown), `fatfs_dir_is_empty`; `vfs_mkdir_secondary`/`vfs_rmdir_secondary` (FAT only — exFAT returns -1 until 1.39.6); `sh_cmd_mkdir`/`sh_cmd_rmdir` ext2-absent branches wired. Validated: fat-write-smoke `mdir` descends `SHKEEP`, `SHRMD` gone, **`fsck.fat -n` clean** (31/31 gates); exfat-write + bites 1–4 no-regression; ext2 untouched. `check.sh` 11/11, build **1,002,800 B** (crossed 1 MB; ceiling 1.2 M). **Next (1.39.6): exFAT `mkdir`/`rmdir`** (dir-set + Directory attr, no `.`/`..`). **1.39.4 RELEASED — VFS generic-write lift, bite 4: `rm` removes FAT/exFAT files**: new `vfs_delete_secondary` (`vfs.cyr`) over `fatfs_delete`/`exfat_delete` (non-ESP-preferring); `sh_cmd_rm` ext2-absent branch routes through it. **Scope confirmed**: FAT/exFAT have `delete` but **no dir-create** (`mkdir`/`rmdir` don't exist in either backend), so this bite ships `rm` only — `mkdir`/`rmdir` split to bite 5 (needs new FAT/exFAT directory-create). Validated: fat-write + exfat-write `rm` shell gates (target gone on disk / in-kernel find-miss) + **`fsck` clean** both; bites 1–3 no-regression; ext2 path untouched. `check.sh` 11/11, `test.sh` 4/4, build **998,312 B**. **Next (1.39.5): `mkdir`/`rmdir`** — implement FAT/exFAT dir-create first. **1.39.3 RELEASED — VFS generic-write lift, bite 3: `touch` + `echo >` write to FAT/exFAT**: the write side. New `vfs_create_secondary` / `vfs_write_secondary` (`vfs.cyr`) dispatch the shell create/write verbs to `fatfs_create`/`exfat_create` + `fatfs_write_file`/`exfat_write_file`; `sh_cmd_touch` + `sh_echo_redirect` ext2-absent branches route through them. Added **non-ESP-preferring** secondary selection (boot FAT ESP vs FAT/exFAT data partition — surfaced by exfat-write-smoke mounting both). Validated: `fat-write-smoke` (`FAT_ALLOW_ESP_WRITE=1`) + `exfat-write-smoke` shell `touch`/`echo>` land on disk, **`fsck.fat`/`fsck.exfat -n` clean**; bite 1/2 `cat`/`ls` no-regression; ext2 path untouched. `check.sh` 11/11, `test.sh` 4/4, build **997,560 B**. **Next (1.39.4): `rm`/`mkdir`/`rmdir`** across FAT/exFAT. **1.39.2 RELEASED — VFS generic-write lift, bite 2: `ls` lists FAT/exFAT**: `sh_cmd_ls`'s ext2-absent branch now falls through to new `vfs_print_dir_secondary()` (`vfs.cyr`) → `fatfs_ls` (converted serial→FB) or new `exfat_print_dir()` (FB-visible name reconstruction mirroring the validated `exfat_find` set-walk; `exfat_ls` only counted). `ls` lists a FAT/exFAT primary's root with names+sizes for the first time; single-primary-FS (ext2 wins when mounted). Validated: `fat-smoke` `ls` → `CATTEST.TXT` name in log; `exfat-smoke` dispatch-clean (full name under `EXFAT_SEED=1`); ext2 `ls` path untouched (change isolated to `ext2_active==0`). `check.sh` 11/11, `test.sh` 4/4, build **994,824 B** (+1,736). **Next (1.39.3): generic create/write** — `touch`/`echo >` reach FAT/exFAT. **1.39.1 RELEASED — VFS generic-write lift, bite 1: `cat` reaches FAT/exFAT**: the arc's first engineering rung. New `vfs_open_secondary(name, len)` (`vfs.cyr`) dispatches the non-ext2 read backends — fat → exfat → initrd — and `sh_cmd_cat` routes its ext2-miss fallback through it, so `cat` reaches a FAT/exFAT volume's root files for the first time (previously ext2-or-initrd only). Read-first by design: additive, **zero-risk to the iron-validated ext2 write/journal path**. Validated end-to-end — `fat-smoke.sh` drives the real `cat CATTEST.TXT` verb via `sh_exec` → `VFS-CAT-FAT-OK` in the boot log; `ext2-write-smoke` W4b (`cat`) no-regression; `check.sh` 11/11, `test.sh` 4/4, build **993,088 B** (+256 B). Arc plan + bite ladder: agnosticos [`vfs-generic-write-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/vfs-generic-write-prior-art.md). **Next (1.39.2): generic `ls`/readdir** — needs `fatfs_ls` (serial→FB) + exFAT name reconstruction. **1.39.0 RELEASED — Cyrius toolchain re-pin 6.0.3 → 6.0.14 (language bump)**: a standalone language-version cut — the deferred re-pin to current Cyrius + the VERSION roll that opens the 1.39.x line. **No kernel engineering** in this tag. The pin was held on 6.0.3 since 1.35.5 (through the 1.37/1.38 big-write arcs); re-pinned to current, **validated byte-identical** (production kernel 992,832 B on 6.0.14, multiboot2 OK, `check.sh` 11/11, `test.sh` 4/4, jbd2 integration write-path smoke PASS — zero codegen impact; drift warning cleared). **1.39.x = VFS generic-write lift** (the shared write path the FAT/exFAT shell verbs + the two now-crash-safe writable filesystems earn) lands in subsequent cuts; specific roadmap tags assigned then. **1.38.11 RELEASED — 1.38.x jbd2 arc CLOSE**: crash-safe journaling iron-validated by the `13810_*` re-burn — write-side `commit_tx: COMMITTED` on the unmodified CSUM_V3 agnos-fs journal + 100-tx stress `PASS (clean shutdown)` + a mid-cycle power cut recovered to `clean journal seq=142`, host `e2fsck -fn` clean at every checkpoint. Cycle-close housekeeping (stale arc-marker comment tidy); build byte-identical **992,832 B**; `check.sh` 11/11, `test.sh` 4/4. **1.38.10 RELEASED — JBD2 CSUM_V2/V3 write+replay** (the iron-burn unlock: the real agnos-fs journal is CSUM_V3 + 64BIT; tag/descriptor/commit checksums implemented on write+replay, Linux-`e2fsck`-oracle-validated; `commit_tx` now COMMITs to the real journal). **1.38.9 RELEASED — Iron-burn automation: CMOS-stamped JBD2 telemetry + host wrappers**: three layers of automation enabling iron-burn outcome reporting **without serial cable** (archaemenid has none per [[feedback_no_serial_on_iron]]). Layer 3 (kernel): new `Jbd2CmosSlot` enum + `jbd2_cmos_write`/`jbd2_cmos_inc` helpers stamp slots 0xA0-0xA4 from JBD2 paths — PROBE_OUTCOME (no-journal/clean/dirty/malformed) at probe-end, REPLAY_ATTEMPT+REPLAY_OUTCOME+REPLAY_TX_COUNT at replay-end, COMMIT_TX_COUNT saturating-incremented per successful commit. `agnosticos/scripts/src/read-boot-log.cyr` extended with a `--- JBD2 iron-burn telemetry ---` section emitting verdict lines + warn-on-inconsistent-state. Layer 1 (host): `agnosticos/scripts/iron-jbd2-prep.sh {production|no-replay|replay|integration|crash}` builds variant + (for replay) pre-stages dirty journal + flashes via install-usb.sh; `agnosticos/scripts/iron-jbd2-validate.sh` reads CMOS via read-boot-log + runs `e2fsck -fn` + parses journal SB on disk. Layer 2 (docs): audit-doc burn-day-flow rewritten with the 3-burn automated cadence (was 4-5 manual burns). `jbd2-replay-smoke.sh` + `jbd2-integration-smoke.sh` regression-clean — CMOS stamps don't break existing paths. Production **986,656 → 987,544 B** (+888). `test.sh` 4/4, `check.sh` 11/11. **1.38.8 RELEASED — JBD2 arc-close hardening + iron-burn pre-audit (CLOSES the 1.38.x JBD2 arc)**: replay-side ingress validation (the only untrusted-input surface of the stack): (1) commit_blk bounded against journal size, (2) tag target_blk bounded against fs_total_blocks, (3) tag target_blk refused if < 4 (FS-SB / early-BGDT range). NO new functional surface — happy-path behavior unchanged; bounds only fire on malformed inputs. Plus the **iron-burn pre-audit doc** in agnosticos ([`ext4-jbd2-iron-burn-audit.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/ext4-jbd2-iron-burn-audit.md)): seven sections incl. six test-item rubric, diff against QEMU (where iron could expose what QEMU's idealized NVMe model couldn't — DMA reordering, FLUSH-CACHE latency, real power-loss durability), and burn-day flow. **User-driven burn per [[feedback_iron_burns_block_other_work]]** — not auto-proposed. `jbd2-replay-smoke.sh` regression-clean (e2fsck `AGNOS-EXT: 12/17152 files, 2146/17152 blocks`). Production **986,184 → 986,656 B** (+472). `test.sh` 4/4, `check.sh` 11/11. **JBD2 arc-close summary** — 8 bites: probe (.0) → probe-deepen (.1) → log reader (.2) → replay (.3) → lifecycle (.4) → write path (.5) → integration (.6) → crash smoke (.7) → hardening + audit (.8). AGNOS now both CONSUMES Linux-left journals AND PRODUCES its own. **1.38.7 RELEASED — JBD2 crash-injection smoke: end-to-end recovery validated**: new `ext2_jbd2_crash_selftest` runs 100 commits in a ~3 s `rdtsc` busy-wait window; new `scripts/jbd2-crash-smoke.sh` iterates N times (default 4), each time SIGKILL'ing QEMU at varied points within the stress window and re-booting agnos against the same image. **PASS 4/4** — all iterations produced e2fsck-clean FS. Honest finding: all 4 kills landed in the busy-wait gap between commits (sync-checkpoint + 3 barriers → ~10 ms per-commit at-risk window vs ~30 ms busy-wait between → 75 % "between commits" zone). Boot 1 markers showed real progress before kill (e.g., `seq=14` for iter-3); boot 2 saw `clean journal` every time. **The framework handles every kill point cleanly** — that's the dispositive recovery guarantee. The specific mid-commit-then-replay path stays validated by `jbd2-replay-smoke.sh` (1.38.3, manually-constructed dirty journal). Production **984,632 → 986,184 B** (+1,552). `test.sh` 4/4, `check.sh` 11/11. **1.38.6 RELEASED — JBD2 integration: metadata writes route through the journal**: `ext2_put_inode`'s inode-table block write now flows through `ext2_jbd2_log_metadata` when a tx is active (via new thin router `ext2_metadata_write_or_log`). New public wrapper `ext2_write_at_journaled(inode, off, src, len)` opens tx → runs `ext2_write_at` (whose internal `put_inode` now routes via journal) → commits on success / aborts on failure (falls back to direct when no journal / dirty / begin fails — safe drop-in). **Narrow-scope limitation**: only the inode-table write is currently routed; block-bitmap / group-desc / sb / extent-tree node writes from the allocation path stay direct. Overwrite-in-place writes get FULL inode-update atomicity; grow operations can split bitmap-and-inode under mid-write crash → e2fsck-fixable orphan blocks (full atomicity deferred to 1.38.7+). Integration selftest (`JBD2_INT_SELFTEST=1`) opens tx → read root inode → put_inode → **verifies tx_count > 0 (the dispositive routing check)** → commit + sync. **`jbd2-integration-smoke.sh` PASS — all 7 gates green** incl. `jbd2-int: put_inode routed through journal (logged 1 metadata blocks)` + `e2fsck -fn clean` + `journal SB s_start=0, s_sequence=2`. Production **982,576 → 984,632 B** (+2,056). `test.sh` 4/4, `check.sh` 11/11. **1.38.5 RELEASED — JBD2 journal write path: AGNOS PRODUCES journals**: `commit_tx` swaps from trace-only to the real 9-step sequence — **descriptor → data blocks → barrier → commit → barrier → checkpoint → barrier → SB-clean**. Three load-bearing FLUSH-CACHE barriers (1.33.5 primitive) gate against SSD write-reordering: torn pre-commit → no commit found, replay discards; torn post-commit pre-checkpoint → replayable journal entry, replay re-applies; torn post-checkpoint pre-SB-clean → still-dirty journal, idempotent re-apply. Sync-checkpoint model — every commit immediately checkpoints + cleans the journal (`s_start=0`); same log space reused. Async checkpoint deferred until multi-threading. New helpers: `ext2_store32_be` / `_be16` (BE stores), `ext2_jbd2_write_log_block` (resolve-and-write to journal logical block), `ext2_jbd2_build_and_write_descriptor` (tag-stream emitter with 64BIT awareness + SAME_UUID/LAST_TAG flag handling), `ext2_jbd2_build_and_write_commit`. CSUM_V2/V3 journals refused cleanly (deferred to 1.38.7 hardening). End-to-end selftest (`JBD2_WP_SELFTEST=1`): reads FS block 300, logs it back, commits + syncs. **`jbd2-writepath-smoke.sh` PASS — all 7 gates green**: kernel trace + `e2fsck -fn clean` (`AGNOS-EXT: 12/17152 files, 2146/17152 blocks`) + journal SB on disk shows `s_start=0, s_sequence=2`. Production **977,792 → 982,576 B** (+4,784). `test.sh` 4/4, `check.sh` 11/11. **1.38.4 RELEASED — JBD2 transaction lifecycle (in-memory scaffold)**: the write-side API ladder rung — `ext2_jbd2_begin_tx` → `ext2_jbd2_log_metadata(blocknr, buf)` → `ext2_jbd2_commit_tx` (plus `abort_tx`). State: `ext2_jbd2_tx_blocks[256]` + `ext2_jbd2_tx_bufs[256]` arrays (4 KiB BSS), `ext2_jbd2_tx_active/_seq/_count/_seq_next` scalars. **Trace-only at 1.38.4** — `commit_tx` lists queued target FS blocks; no on-disk journal writes (1.38.5's descriptor + data + commit + FLUSH-CACHE plugs into the same call-site contract). The lifecycle's seq allocator (`tx_seq_next`) is seeded from the journal's next-expected sequence at mount AND from the replay outcome, so post-replay tx's continue the monotonic seq. Borrowed-buffer model: caller's source buffer must stay live `log_metadata` → `commit_tx`; fine because the integration at 1.38.6 will pass long-lived kernel buffers (`ext2_inode_buf`/`ext2_dir_buf`/`ext2_extent_buf`). Compile-gated boot self-test (`JBD2_TX_SELFTEST=1`) exercises 3 positive + 3 negative-path error responses. **`jbd2-tx-smoke.sh` PASS** — all 7 trace gates green incl. negatives. Production **970,920 → 977,792 B** (+6,872). `test.sh` 4/4, `check.sh` 11/11. **1.38.3 RELEASED — JBD2 replay-on-mount (the unlock)**: dirty-journal mount now APPLIES the parsed transactions. `ext2_jbd2_replay_one_tx()` reads descriptor → parses tags into `ext2_jbd2_tag_buf[256]` (packed `(flags << 56) | target_blk_56bit`) → reads commit block → validates magic + blocktype + sequence-matches-descriptor BEFORE any data writes (validate-first, apply-second — a torn transaction is discarded, never half-applied) → copies each data block from journal log position → FS target via `ext2_write_block` (with `JBD2_FLAG_ESCAPE` restore for blocks whose first 4 bytes were zeroed). `ext2_jbd2_rewrite_sb_clean()` rewrites the journal SB with `s_start = 0` + advanced `s_sequence` (recomputing CRC32C if `CSUM_V2/V3` is set), resolves the journal's first FS block via `ext2_logical_to_physical`, writes in-place via `ext2_write_block`, and issues `blk_flush_on(ext2_backend)` (1.33.5 FLUSH-CACHE barrier — the clean-journal state must reach non-volatile media before we declare success). `ext2_jbd2_replay()` top-level walks transactions + on success calls `ext2_sync` (re-asserts `EXT2_VALID_FS` so `e2fsck -fn` sees cleanly-unmounted) + lifts `ext2_write_ok = 1`. **`jbd2-replay-smoke.sh` PASS, all 5 gates including `e2fsck -fn` clean post-replay**: `AGNOS-EXT: 12/17152 files (8.3% non-contiguous), 2146/17152 blocks`. Python helper extended with `data_source="block"` mode that reads target FS block content first so replay is a byte-identical no-op (e2fsck stays clean regardless of which FS block is targeted). Compile gate `JBD2_NO_REPLAY=1` skips replay so 1.38.0's refusal-only path stays regression-testable. Production **959,272 → 970,920 B** (+11,648). `test.sh` 4/4, `check.sh` 11/11. **1.38.2 RELEASED — JBD2 log-format reader (descriptor / commit / revoke walker)**: the dirty-journal mount path now PARSES the log — `ext2_jbd2_log_walk_and_trace()` walks from `s_start` through journal blocks decoding the BE headers (magic `0xC03B3998`, blocktype 1=descriptor / 2=commit / 5=revoke, sequence), variable-length tag streams (`t_blocknr_lo`+`t_flags`+`t_checksum`, optional `t_blocknr_hi` under 64BIT, optional UUID for first-or-non-SAME_UUID), and emits a `debugfs logdump`-style trace. Non-mutating; halts on the first malformed block (where 1.38.3 replay will stop applying). Two invocation paths: compile-gated automatic at mount (`JBD2_LOGDUMP=1 sh scripts/build.sh`) or on-demand via the `jbd2` shell verb when dirty. Test infrastructure: `mk-dirty-journal-img.py --synth-tx [target_blk]` synthesizes a complete 1-transaction journal at log blocks `[1..3]` (descriptor + 4 KiB data + commit, no V2/V3 csum); `jbd2-logdump-smoke.sh` gates the trace against 5 expected lines + the shell-came-up signal. **smoke PASS first attempt**: trace reads exactly `walk start=1 seq_expected=1 → DESCRIPTOR seq=1 at blk=1 → tag: dest_blk=100 flags=0x8 → COMMIT seq=1 at blk=3 → end at blk=4 (no magic; 1 complete tx)`. Production **956,168 → 959,272 B** (+3,104; walker fn body always compiled, only the auto-mount invocation is compile-gated). `test.sh` 4/4, `check.sh` 11/11. **1.38.1 RELEASED — JBD2 probe deepens + dirty-image test infrastructure**: completes the journal-SB read surface (s_first / s_feature_*  / s_nr_users / s_checksum_type / s_checksum), adds conditional CRC32C-V2/V3 SB-csum validation (gated on `JBD2_FEATURE_INCOMPAT_CSUM_V2/V3`), adds a `jbd2` shell verb for diagnostic continuity, and ships the test infrastructure 1.38.2+ will reuse: `scripts/mk-dirty-journal-img.py` (Python helper that writes `s_start != 0` to the journal SB, recomputing the SB csum if CSUM_V2/V3 is on) + `scripts/jbd2-refusal-smoke.sh` (boots agnos against the dirty image, gates the DIRTY diagnostic + refusal reason + shell-reaches-prompt). **`jbd2-refusal-smoke.sh` PASS** on first attempt: `jbd2: DIRTY journal ino=8 start=1 seq=1 -- refusing RW mount` emitted, then shell `v1.38.1` came up cleanly (RO mount allowed, not a hang). The refusal path is now end-to-end-validated against a real dirty-journal image. Production **952,968 → 956,168 B** (+3,200). `test.sh` 4/4, `check.sh` 11/11. **1.38.0 RELEASED — jbd2 journaling arc opened (audit-doc + dirty-mount stop-gap)**: second of the heavy big-write cycles. 1.33.x reached file-write via no-journal ordered-write; 1.37.x added extent-allocation; neither protects against a yanked power mid-update. JBD2 closes that gap — write multi-block metadata changes to a separate log file first, replay any committed-but-not-checkpointed entries on mount. AGNOS today silently ignores the journal — fine when nothing crashes, broken when AGNOS mounts a Linux-left-with-pending-log FS (silent stomp). 1.38.0 closes that second hole as a stop-gap: **`ext2_jbd2_probe`** reads the journal SB at mount; clean (`s_start == 0`) → log `jbd2: clean journal ino=N size=M seq=K` + RW allowed; dirty → log + downgrade `write_ok = 0` (RO stays); malformed → refuse mount. Big-endian readers (`ext2_load32_be`) added since JBD2 headers are BE throughout per Tweedie 1998. QEMU smoke emits `jbd2: clean journal ino=8 size=1024 seq=1` (1024 = 4 MiB at 4 KiB blocks on the 67 MiB smoke partition; the real 25 GiB agnos-fs partition emits `size=8192` = 32 MiB). User-directed narrow mode (metadata-only, no `data=ordered`). Audit + ladder: agnosticos [`ext4-jbd2-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/ext4-jbd2-prior-art.md) §5 — **1.38.0 audit+probe+dirty-refusal · 1.38.1 log-format reader · 1.38.2 replay-on-mount (lifts refusal) · 1.38.3 in-memory transaction lifecycle · 1.38.4 journal write path · 1.38.5 ext2_write_at integration · 1.38.6 QEMU crash-injection smoke · 1.38.8 arc-close hardening · iron burn user-driven after .8**. Production **945,360 → 952,968 B** (+7,608); `test.sh` 4/4, `check.sh` 11/11. **1.37.5 RELEASED — kashi 0.6.0 fold-in arc-close**: the 1.37.x extent-allocation arc closes by retiring the inline 96-glyph VGA 8x16 tables in `kernel/arch/x86_64/fb_console.cyr` and consuming kashi's freestanding font-data core (`../kashi/src/font_data.cyr`) instead. kashi 0.1.0 was extracted byte-for-byte from agnos's tables 2026-05-27 and has since rocketed to 0.6.0 (PSF1/PSF2 import, runtime registry, CP437 widening, hardening audit) under a parallel agent per [[project_kashi_parallel_split]]. This cut IS the consumption-side fold-in — what was previously booked at agnos 1.38.0 lands at the 1.37.x close instead, and the 1.40.x font-separation slot is freed. Mechanism: `[deps.kashi] path="../kashi" modules=["src/font_data.cyr"]` in `cyrius.cyml` (documents the contract); `scripts/build.sh` + `test.sh` prepend `../kashi/src/font_data.cyr` to the kernel source before `cyrius build --no-deps` (the mechanism — cyrius dep-walking would require cwd reshuffling against existing relative-include resolution). `fb_console.cyr` now calls `kashi_font_init()` once at boot and `kashi_glyph_ptr(KASHI_FONT_VGA_8X16, ch)` per glyph render with a null-pointer guard. Glyph rendering byte-identical to 1.37.4 by construction (kashi 0.1.0 was extracted byte-for-byte; 0.2.0–0.6.0 only widened the encoded range). Production **843,776 → 945,360 B** (+101,584 — kashi vendors full CP437 + 8x8 + 9x16 derived face + accessors beyond what agnos uses; `CYRIUS_DCE=1` would reclaim ~48 KB of unused-fn bloat, deliberately not enabled this cut to keep build behavior identical). `test.sh` 4/4, `check.sh` 11/11. Roadmap rows 33 + 36 rewritten: 1.38.x opens with **jbd2 journaling** as the headline (the previously-booked 1.38.0 kashi-consumption landed here at 1.37.5); 1.40.x slot freed. **1.37.4 RELEASED — ext-extent selftest idempotent re-boot skip**: iron Attempt 1373 boot-1 PASSED depth-2 + `e2fsck -fn /dev/nvme0n1p2` clean, then boot-2 against the same NVMe emitted `ext-ext: no sibling leaf formed FAIL` — test-not-idempotent, NOT a filesystem bug. `ext2_extent_write_selftest` now recognizes `eh_depth==2` from a prior boot and emits `ext-ext: /extseed.dat already at depth=2 (prior boot) -- skip PASS`. QEMU two-boot test green; Iron Attempt 1373 closes as the 1.37.x arc's iron validation. **1.37.3 RELEASED — ext4 extent depth-2 tree growth**: when all 4 inline-root index slots fill (4 full leaves), the tree grows to depth 2 — `ext2_extent_grow_indepth2` spills the root's 4 index entries into an INDEX block (+ its metadata_csum node checksum), the root becomes a single index entry, and appends descend root → index block → leaf (`ext2_extent_add_leaf_to_idxblk` adds sibling leaves under the index block, held in `ext2_indirect_buf_l1`). Completes the on-demand grow ladder **0→1→2** — the allocator now handles every realistic file shape (sibling index blocks / depth-3 deferred, unreachable: ~115 k extents). Validated: `ext-extent-smoke.sh` drives the tree to `eh_depth==2` (`final depth=2 root_entries=1 size=11.1 MB`, ~1360 extents across 4 leaves + the index block) → **`e2fsck -fn` clean** (proves both grows + every sibling split + leaf AND index-node checksums + all index entries + inode csum + i_blocks). Production **842,840 B**; `test.sh` 4/4, `check.sh` 11/11. **Remaining: the arc's iron burn (user-driven)** — extent allocation's first real-hardware touch (depth-0/1 covers every realistic file); test-item rubric + prep in agnosticos [`iron-nuc-zen-log.md#tracker-1373-cycle`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-nuc-zen-log.md) (flash the ready selftest `build/agnos` via `install-usb.sh --update`; the kernel **self-seeds** `/extseed.dat` → FB `depth-2 PASS` → host `e2fsck -fn` clean — no host-side seeding). ⚠ `ext2-write-smoke` symlink tests (`/sl_f`/`/sl_s` type='') fail **PRE-EXISTING** (present at HEAD without depth-2 — confirmed by stash-test; an indirect-path symlink-create issue, not an extent regression) — flagged for a separate look. **1.37.2 RELEASED — multi-leaf (depth-1 sibling split)**. When the depth-1 leaf fills, `ext2_extent_add_sibling_leaf` allocates a new leaf + adds a root index entry (up to 4 leaves ≈ 4×eh_max extents); the append path decides extend/insert/sibling per-write. Validated: `ext-extent-smoke.sh` drives the leaf to overflow → `eh_depth==1` `eh_entries==2` (2 leaves, ~2.7 MB sparse) → **`e2fsck -fn` clean** (proves grow + sibling split + both leaf checksums + root index + inode csum); `ext2-write-smoke` no-regression. **1.37.1 RELEASED — depth 0→1 grow** (`ext2_extent_grow_indepth` + leaf-node checksum; e2fsck-clean). **1.37.0 RELEASED — extent depth-0 append**: `ext2_extent_append_block` (extend-last + insert into inline root ≤ 4) + `ext2_write_at` routing by `EXTENTS_FL`; e2fsck-clean. Audit: agnosticos [`ext4-extent-alloc-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/ext4-extent-alloc-prior-art.md). (Versioning structure Claude-determined per the 2026-05-27 delegation; releases are user-tagged.) **1.36.2 RELEASED — `main.cyr` selftest extraction** (declutter the boot orchestrator: the trailing compile-gated boot self-tests → `core/selftests.cyr` and the kybernet launch → `core/boot_finish.cyr`; `main.cyr` 1661 → 1244 LOC, the boot-init sequence. Pure reorg: **production byte-identical** (sha `18e23876…`, 828,528 B); ordering confirmed — selftests run after init, before launch: dns 3/3, hardening 1/1, tcp-listen 2/2; test/check green. The 1.36.x refactor cycle's planned targets — net.cyr split (1.36.0/1) + main.cyr declutter (1.36.2) — are done; `ext2`→1.39.x, `shell`→1.41.x deferred.) **1.36.1 — `net.cyr` split COMPLETE (part 2: app-protocols + ingress)** (split `net.cyr` from a 2019-LOC catch-all to a 272-LOC L2/L3 core; the per-protocol layer → `net_dhcp`/`net_icmp`/`net_dns`/`net_ntp`/`net_rtc.cyr` and the receive/dispatch path → `net_ingress.cyr`. With `net_tcp.cyr` (1.36.0) the stack is now 8 focused files. Pure source reorg: **build byte-identical** (sha `512734b3…`, 828,528 B), behavior provably unchanged; full net smoke suite green — dns 3/3, icmp/ntp/rtc 1/1, tcp 4/4, tcp-listen 2/2. The 1.36.x refactor headline is **done**; `ext2`→1.39.x, `shell`→1.41.x. Plan: agnosticos [`refactor-net-cyr-split.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/refactor-net-cyr-split.md).) **1.36.0 RELEASED — `net.cyr` split part 1 (TCP)** (TCP stack → `net_tcp.cyr`, ~780 LOC; byte-identical build). **1.35.7 RELEASED — arc-close hardening (passes 1 + 2)** (security: pass 1 — `net_poll` clamps the attacker-controlled IPv4 total-length to the actually-received frame via `ip_safe_payload_len`, closing a forged-length over-read that let ICMP echo reflect stale bytes / UDP+TCP over-read `net_rx_pkt`; pass 2 — TCP seq-wrap masks + RTC year bound; non-structural, no valid-traffic change; `hardening-smoke.sh` green + net smokes no-regression; audit agnosticos [`arc-close-hardening-1-35.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/arc-close-hardening-1-35.md)). **1.35.6 RELEASED — DNS cache** (8-entry TTL-respecting positive cache + TTL extraction; `dns_cache_find`/`dns_cache_put`; lwIP-style evict-soonest; repeated `ping`/`ntp <host>` no longer re-query; `dns-smoke.sh` 3/3 incl. `dns: cache PASS`; audit agnosticos [`dns-stub-resolver-prior-art.md` § 9](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/dns-stub-resolver-prior-art.md)). Multi-A/CNAME + retransmit were already shipped at 1.35.0; this was the cache gap. **1.35.5 RELEASED — RTC boot clock** (reads the CMOS RTC at boot to seed a wall clock with no network — `civil_to_unix` + `rtc_read_unix`; `date` shows `[RTC]`/`[NTP]`; NTP refines/overrides; `rtc-smoke.sh` green; audit agnosticos [`rtc-boot-clock-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/rtc-boot-clock-prior-art.md)). Cycle also moved the kernel cyrius pin **6.0.1 → 6.0.3** after a byte-identical kernel A/B. **1.35.4 RELEASED — `munmap`** (syscall 28; releases an anonymous region + returns its physical 2 MB pages to the PMM via `proc_unmap_page` + `invlpg` + `pmm_free_2mb`; LIFO arena reclaim; `mmap-smoke.sh` 2/2; closes the mmap/munmap pair). **1.35.3 RELEASED — anonymous `mmap`** (syscall 27, 2 MB-granular zero-filled memory into the caller's address space; new `pmm_alloc_2mb` contiguous allocator; audit agnosticos [`mmap-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/mmap-prior-art.md)). The first new *functional* syscalls since v1.21.0 — pure memory facilities, no socket/crypto surface. **1.35.2 RELEASED — NTP/SNTP** (the kernel's first wall clock from a one-shot SNTP query; `ntp` + `date` verbs). **1.35.1 RELEASED — TCP hardening** B0–B4 (in-order receive ring + retransmit/RTO + MSS/segmentation + peer-window; the minimal SYN/ACK/FIN machine is now a reliable, flow-controlled stream). **1.35.0 RELEASED** — the catchup-tidbits cut: a full docs sweep + **DNS stub resolver** (`dns` verb; `dns-smoke.sh` green, live `example.com` via SLIRP; audit agnosticos [`dns-stub-resolver-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/dns-stub-resolver-prior-art.md)) + **ICMP echo / ping** (`ping` verb, pingable + `icmp_ping`; `icmp-smoke.sh` green — hermetic checksum + live gateway round-trip). The planned AGNOS-side comms order (DNS → ICMP → TCP-hardening → NTP) is **COMPLETE**; TLS + PIE are the cyrius-side next destinations (days-to-weeks). Legacy virtio-net back-burnered (known TX gap); plug-and-play / hot-add still a candidate. **Last release: agnos 1.37.0.** The **1.34.x FAT-family arc** ([`roadmap.md`](roadmap.md) row 21) shipped FAT12/16/32 + exFAT read+write across **1.34.0–1.34.6** (FAT read+write → exFAT read+write → write parity → LFN/truncate → directory growth → exFAT Unicode names → ESP-write guard) — all RELEASED + `fsck`-clean in QEMU. **Only the user-driven FAT/exFAT iron burn remains** (the arc's first iron touch — the guard makes it brick-safe; plan in agnosticos `#tracker-1341-cycle`). Production build **838,048 B** (~818 KB; 1.37.x extent allocation: depth-0 append + depth-0→1 grow + multi-leaf sibling split); `test.sh` 4/4 + `check.sh` 11/11 (binary-size sanity ceilings at 1.2 M), ext2 + ext-extent + FAT + exFAT + dns + icmp + tcp + ntp + mmap + rtc + hardening smokes green. **MVP gate (boot-to-shell with typeable keyboard) green on archaemenid since Attempt 68 / 1.30.9.** **Closed arcs (history → [`CHANGELOG.md`](../../CHANGELOG.md) + the iron-log, not here):** storage backends + GPT + ext2/4 read (1.31.x), networking incl. r8169 unicast-RX + DHCP iron-verified (1.32.x), ext2/4 WRITE incl. the W5 demo→base iron burn + fsync barrier (1.33.x). **Forward plan beyond 1.34.x** — big-write own-cycles (1.37 extent / 1.38 jbd2 / 1.39 VFS) → kernel-slimming (1.40 font→`kashi` / 1.41 shell→agnoshi) → 1.42 perf → 1.43–1.45 agnos-2.0 runway, + platform decades (1.5x Intel / 1.6x Pi / 1.7x radios / 1.8x RISC-V) — lives in [`roadmap.md`](roadmap.md). 1.30.x AMD Zen scanout residue stays parked per `project_amd_zen_scanout_residue`. | **Refresh cadence**: every release, ideally via `scripts/version-bump.sh` (it refreshes the header date + Version row + roadmap "Current" line; body prose needs a manual sweep at minor closeouts — like this one).
>
> **Scope**: live snapshot of this repo (`agnos`). Volatile state lives here so [`CLAUDE.md`](../../CLAUDE.md) can stay durable. Historical narrative lives in [`CHANGELOG.md`](../../CHANGELOG.md); the design ledger lives in [`roadmap.md`](roadmap.md). Iron-bring-up per-attempt detail lives in [agnosticos `iron-nuc-zen-log.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-nuc-zen-log.md).

---

## Version

| Field | Value | Source |
|---|---|---|
| **Kernel** | **1.41.8** | [`VERSION`](../../VERSION) |
| **Cyrius toolchain pin** | **6.0.56** | `cyrius.cyml [package].cyrius` |
| **Released** | 2026-06-04 | [`CHANGELOG.md`](../../CHANGELOG.md) |
| **Iron-validated** | 2026-05-25 (archaemenid NUC AMD — **MVP gate green since Attempt 68 / 1.30.9**; **1.32.x networking arc iron-COMPLETE**: r8169 unicast-RX solved at 1.32.7 + DHCP real lease `.142` iron-verified at 1.32.9; storage trio + GPT + ext4 + shell byte-clean). The 1.33.x ext2/4-WRITE + 1.34.x FAT-family arcs are QEMU/`fsck`-validated; their final-bite iron burns stay user-driven (pending). | NUC AMD Attempts 68 (MVP gate) + 71-77 (FB) + 80-91 (storage arc) + 92+ (networking arc — DHCP iron-verified 1.32.9) |

## Open investigations

The kernel cleared boot-to-shell-on-iron at Attempt 68 (agnos 1.30.9) and stays green; the 1.31.x storage + 1.32.x networking arcs are iron-COMPLETE (DHCP real lease iron-verified at 1.32.9, agnosticos [`#tracker-1329-cycle`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-nuc-zen-log-mvp2.md#tracker-1329-cycle)). **No MVP blockers.** One live item:

- **AMD Zen scanout residue (Quiet Boot legibility)** — parked at gnoboot 0.4.2 / agnos 1.30.12 with the bug surviving. Two GOP-side SetMode lever variants falsified (Attempt 78 closed the bounce form). Next-cycle resumption options: HUBP `clear_tiling` port (Linux `drivers/gpu/drm/amd/display/` analog) OR architectural eval of a shadow-buffer FB-console model (simpledrm-style). Pin: [`project_amd_zen_scanout_residue`](../../../../.claude/projects/-home-macro-Repos-agnosticos/memory/project_amd_zen_scanout_residue.md). VGA-spec path stays MVP-legible.

### Resolved (historical, kept as audit trail)

- **2026-05-13 RDRAND under default qemu64 CPU**: kernel stalled at `Page tables: 1024MB mapped` because the smoke test missed `-cpu max` (default `qemu64` lacks RDRAND, `pmm_init` → `kaslr_seed` → `rdrand_u64` faulted silently). Real iron supports RDRAND. Fixed in `gnoboot/tests/ovmf_smoke.sh` with `-cpu max`.
- **2026-05-13/14 Timer-driven context switch under UEFI+gnoboot**: traced to `test_proc_a/b` returning into uninitialized stack memory exposed by gnoboot's pre-handoff state. Closed by Phase 4/5 progression (real user procs replaced test stubs in the boot path) and the iron-validation milestone 2026-05-15 which cleared all 17 init checkpoints + `sched_active=1` + first hlt + context-switch loop on real Zen silicon.
- **2026-05-17 Phase 3 USB silent-absorb (Repair EE)**: 13-hypothesis arc through Attempts 32-54 chasing a "controller absorbs PORTSC.PR writes" hypothesis. Root cause: `xhci_portsc_write` inner re-mask `& XHCI_PORTSC_NEUTRAL` stripping the RW1S PR bit. One-line fix in `agnos@41ee6dc`. See CHANGELOG [1.30.5] for the narrative.
- **2026-05-18 xHCI Enable Slot CCE silent-absorb (Repair QQ + cyrius gvar-init-order fix)**: 9-letter spec-path repair ladder (FF→OO) falsified Attempts 57-62 on AMD FCH 1022:1639. Resolution came from the cyrius side: v5.11.64 fixed a kmode init-order bug where `var X = INT_LITERAL` at module scope read as 0 before the init block ran, causing silent-absorb on the cmd-path. Iron-validated at Attempt 68 (1.30.9) — typeable shell on archaemenid → MVP gate hit. See agnosticos `iron-nuc-zen-log` § Attempt 68 + cyrius `issues/2026-05-18-gvar-init-order-zero-reads.md`.
- **2026-05-20 NVMe arc + iron debut**: Phase 1-5 driver shipped under [1.31.0]; iron debut on Crucial P3 2 TB at Attempt 80 was first-try clean. The contrast with xHCI's 5-week / 19-attempt / 9-letter-code path is the structural reading on `feedback_redesign_dont_reinvent`: port from Linux's `drivers/nvme/host/pci.c`, redesign to Cyrius conventions, get a clean iron debut.
- **2026-05-25 networking arc close (r8169 unicast-RX → DHCP)**: the 1.32.3 `dhcp: OFFER timeout` was NOT a DHCP-layer bug — it was the r8169 RX ring dropping clean unicast frames for want of a free descriptor (16-deep ring). Fixed at **1.32.7** (RX ring 16→64; `missed` 176→0; on-LAN + off-LAN TCP handshakes complete on iron), DHCP re-enabled at **1.32.9** with a real lease `.142` iron-verified. The whole 1.32.x networking arc is COMPLETE. agnosticos [`#tracker-1329-cycle`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-nuc-zen-log-mvp2.md#tracker-1329-cycle) + CHANGELOG [1.32.x].

The full history of these investigations lives in [`CHANGELOG.md`](../../CHANGELOG.md) and the agnosticos iron-nuc-zen-log. This `state.md` section tracks **live** investigation only — historical investigations should resolve here and migrate to CHANGELOG.

---

## Build artifacts

Sizes from `wc -c` on `build/agnos*` after `scripts/build.sh`, cyrius 6.0.56, default DCE.

| Arch | Binary | Size | Notes |
|---|---|---|---|
| x86_64 | `build/agnos` | **1,084,312 B** (at 1.41.8) | ELF64 multiboot2 (Path C — sovereign UEFI boot-info ABI via gnoboot v0.4.2; RDI = `&boot_info`, magic `0x41474E4F`), entry `0x1000a8`. Boots under `qemu-system-x86_64 -cpu max` + OVMF + gnoboot. Iron-validated on archaemenid: MVP gate Attempt 68 (1.30.9), storage trio Attempts 80/81/87, ext4 victory lap 90/91, networking arc through Attempt ~100 (DHCP iron-verified 1.32.9). |
| aarch64 | `build/agnos-aarch64` | deferred — separate work area | Compile-only, no boot harness; **not gated** (test.sh/check.sh/CI are x86-only). During first-primary-kernel buildup the **AMD x86 line (archaemenid) is the one and only kernel** — aarch64 bring-up (incl. stub parity) is its own future arc (decade 1.6x), not maintained in lockstep here. x86 is the only build the target line depends on. |

Per-cut size trajectory + deltas live in [`CHANGELOG.md`](../../CHANGELOG.md) (the at-a-glance ledger — this is current truth, not a log). Bookmarks: ~249 KB (1.28.x) → **~395 KB v1.30.9 MVP gate** → 1.31.x storage arc → 1.32.x networking → 1.33.x ext2/4 write → 1.34.x FAT-family → 798,936 B (1.34.6) → 1.35.x comms (DNS/ICMP/TCP/NTP) + mmap/munmap + RTC + DNS-cache + ingress hardening → **828,464 B (1.35.7)**.

---

## Source rollup

| Tree | Files | Notes |
|---|---|---|
| `kernel/` (total) | **80** `.cyr` | ~27,000 lines across all kernel sources. The 1.36.x refactor split `net.cyr` (2019 → 272 LOC) into 8 focused files (`net.cyr` core + `net_dhcp`/`net_icmp`/`net_dns`/`net_ntp`/`net_rtc`/`net_ingress`/`net_tcp`) and `main.cyr` (1661 → 1244 LOC) into `main.cyr` (boot init) + `selftests.cyr` + `boot_finish.cyr`. Pure reorg, byte-identical builds. |
| `kernel/agnos.cyr` | 1 | Main orchestrator — only `#ifdef` + `include` |
| `kernel/kernel_hello.cyr` | 1 | Minimal smoke test |
| `kernel/klib/` | 3 | `kstring.cyr`, `kfmt.cyr`, `ktagged.cyr` — vendored kernel-safe stdlib |
| `kernel/arch/x86_64/` | 17 | boot_shim, boot_data, fb, fb_console, mbi, serial, gdt, idt, pic, apic, smp, keyboard, paging, io, syscall_hw, ring3, iommu |
| `kernel/arch/x86_64/usb/` | 9 | xhci, xhci_regs, xhci_ring, xhci_cmd, xhci_ctx, xhci_port, hid, hid_translate, msc (USB Mass Storage) |
| `kernel/arch/aarch64/` | 9 | boot_data, serial, gic, timer, exceptions, keyboard, paging, stubs, main |
| `kernel/core/` | **26** | pmm, vmm, heap, proc, sched, syscall, vfs, devs, initrd, kprint, main, pci, acpi, elf; **net, virtio_net, r8169** (networking); **block, nvme, ahci, virtio_blk, ramdisk, gpt** (storage); **ext2, fatfs, exfat** (filesystems) |
| `kernel/user/` | 4 | shell, init, test, test_procs |
| `kernel/version.cyr` | 1 | Auto-generated banner strings — `scripts/version-bump.sh` regenerates |

---

## Subsystem status (40+)

All subsystems are **code-complete** through 1.34.6 (1.35.0 cycle just opened). **MVP gate cleared on iron** — typeable shell at Attempt 68 (1.30.9). Since then: the **1.31.x storage arc** (NVMe / AHCI / USB-MS / RAM-disk / VirtIO-blk + 5-backend block layer + GPT + ext2/ext4 read) closed with iron debuts at Attempts 80/81/87/90/91; the **1.32.x networking arc** (TCP/UDP server primitives + DHCP + r8169 NIC) is iron-COMPLETE (DHCP lease verified 1.32.9); the **1.33.x ext2/4 WRITE arc** is iron-validated (persist-across-reboot); the **1.34.x FAT-family arc** (FAT12/16/32 + exFAT read+write + ESP-write guard) is QEMU/`fsck`-validated. The README § Subsystems is the full enumeration; the roadmap's arc ledger is the at-a-glance index; this table is the shipped-surface detail.

| Subsystem | Notes |
|---|---|
| Boot (multiboot2, 32→64 shim) | 32-bit ELF entry, long mode transition (x86_64) |
| Boot (aarch64) | DTB, EL2→EL1, PL011 UART, GIC, ARM timer |
| Boot (Path-C sovereign UEFI) | gnoboot v0.4.2 hands off via `RDI = &boot_info` (magic `0x41474E4F`); replaces multiboot2-via-GRUB |
| Framebuffer console | GOP handoff capture, WC remap, pitch-aware u64 block-copy paint, 8x16 VGA BIOS-ROM glyph set (true-font swap at 1.30.12) |
| Serial I/O | COM1 `0x3F8` (x86_64), PL011 UART (aarch64) |
| GDT | 5 segments + TSS descriptor |
| TSS | Ring 3 transitions, RSP0 |
| IDT | 256 vectors, default `iretq` handler |
| PIC | 8259A, ICW1–4, remap to INT 32+ |
| Local APIC | MMIO at `0xFEE00000`, timer, IPI |
| GIC | ARM GICv2 interrupt controller (aarch64) |
| Timer | APIC periodic ~100 Hz (x86_64), ARM generic timer (aarch64) |
| Keyboard (PS/2) | Full US QWERTY (x86_64), UART RX (aarch64) |
| Keyboard (USB-HID via xHCI) | Full Phase 1-5 boot kbd driver — `hid_kbd_configure`, `hid_poll`, HID→PS/2 mapping, kb_buf writer; iron-typeable at Attempt 68 |
| Page Tables | 2 MB huge pages, 4 GB identity map, per-process |
| PMM | Bitmap, 4,096 pages, next-free hint |
| VMM | map/unmap/alloc, user-accessible pages, UC + WC remap helpers |
| Kernel Heap | Slab allocator, 8 size classes (32–4,096 B) |
| Process Table | 16 slots, 168 B context, CR3 per-process |
| Context Switch | Full register save/restore, CR3 switch |
| Scheduler | Round-robin |
| SYSCALL/SYSRET | MSR setup, ring 3 transition |
| ELF Loader | Static ELF64, per-process address space |
| VFS | File table, device/memfile/signalfd/epoll/timerfd/pipe types |
| Device Drivers | Serial char device |
| Initrd | Flat format, name lookup |
| PCI Bus | Config space scan, device discovery, 64-bit BAR support |
| ACPI | RSDP scan, DMAR parsing (VT-d), basic table layout |
| IOMMU (VT-d) | Root/context/IO page tables, DTE registration for device DMA |
| VirtIO-Net | Legacy PCI, virtqueues, Ethernet frames (QEMU NIC) |
| **r8169 NIC** | Realtek RTL8111/8168/8169 GbE driver — RX/TX descriptor rings (64/RX, TX-split mask), unicast filter, iron-validated on archaemenid (RX isolated-construction fix 1.32.7) |
| IP/UDP Stack | ARP, IPv4, UDP send/recv |
| TCP Stack | Connect, send, recv, close, SYN/ACK/FIN state machine + **server primitives** (listen/accept) |
| **DHCP client** | DISCOVER → OFFER → REQUEST → ACK lease acquisition; iron-verified lease on archaemenid (1.32.9) |
| VirtIO-Blk | Legacy PCI, sector read/write, DMA buffers |
| **NVMe** | Full Phase 1-5 driver — probe + admin queue + I/O queue + R/W DMA + PRP1/PRP2/PRP-list dispatch. Iron-debut clean on Crucial P3 2 TB at Attempt 80 (1.31.0) |
| **AHCI/SATA** | Full Phase 1-4 driver — HBA probe + per-port CL+FIS bring-up + IDENTIFY DEVICE + READ/WRITE DMA EXT. QEMU-validated on q35 ich9-ahci; iron-validated on archaemenid (Attempt 87) |
| **Block-layer dispatch** | `kernel/core/block.cyr` — tag-based 5-backend dispatch (`BLK_VIRTIO` / `BLK_NVME` / `BLK_AHCI` / `BLK_USB` / `BLK_RAM`); NVMe overrides virtio; AHCI/USB register as secondary when NVMe present (1.31.x) |
| **GPT partition parser** | Full Phase 1-3 — header probe + signature decode + full 16 KB array walk + UTF-16LE name extraction + `parts` shell command + `gpt_partition_info(idx)` helper + table-less CRC32 (0xEDB88320) validation + backup-header recovery + 7-GUID type classifier (ESP / MSFT Basic / Linux FS / Linux Swap / Linux LVM / Linux RAID / BIOS Boot) (1.31.1) |
| **USB Mass Storage** | `usb/msc.cyr` — Bulk-Only Transport, SCSI READ/WRITE(10), block-layer backend (`BLK_USB`) |
| **RAM-disk** | `core/ramdisk.cyr` — in-memory block backend (`BLK_RAM`) for test/seed images |
| **ext2 / ext4** | Read **and write** — inode/block-group walk, directory ops, file create/write/truncate; persist-across-reboot iron-validated (1.33.x WRITE arc, Attempts 90/91) |
| **FAT12 / FAT16 / FAT32** | Read **and write** — cluster-chain walk, LFN (read + spanning-append write), file create/overwrite/truncate/delete, cluster allocator; `fsck.fat` + `mtools` validated (1.34.x) |
| **exFAT** | Read **and write** — 32-bit FAT, allocation bitmap, up-case table (RLE), typed dir-sets (File / Stream-Ext / File-Name), SetChecksum/NameHash; root extension + spanning-append; `fsck.exfat` validated (1.34.x) |
| **FS-write safety guard** | Refuses FAT/exFAT writes on ESP-type GPT partitions (boot ESP protection); `FAT_ALLOW_ESP_WRITE` compile-override for QEMU test images (1.34.6) |
| Pipes | Circular buffer IPC, read/write ends, VFS type 6 |
| SMP Infrastructure | APIC, IPI, trampoline, per-CPU stacks |
| Shell | 28 commands (storage/FS/net arcs added `parts`, mount/ls/cat/write/rm/mkdir over the FS backends, net diagnostics) |
| kybernet Init | PID 1 |
| Signals | per-process `proc_signals` / `proc_sigmask`, `kill`, `sigprocmask`, `signalfd` |
| Epoll + Timerfd | `epoll_{create,ctl,wait}`, `timerfd_{create,settime}` |

### Syscall surface (28 functional + 1 diagnostic = 29 dispatch entries)

`exit`(0), `write`(1), `getpid`(2), `spawn`(3), `waitpid`(4), `read`(5),
`close`(6), `open`(7), `dup`(8), `mkdir`(9), `rmdir`(10), `mount`(11),
`sync`(12), `reboot`(13), `pause`(14), `getuid`(15), `kill`(16),
`sigprocmask`(17), `signalfd`(18), `epoll_create`(19), `epoll_ctl`(20),
`epoll_wait`(21), `timerfd_create`(22), `timerfd_settime`(23),
`umount`(24), `pipe`(25), **`mmap`(27)**, **`munmap`(28)**.

Slot **26** is `write_boot_checkpoint(byte)` — a CMOS-write diagnostic from
iron-boot bring-up, not part of the functional set. The 26-call kybernet
surface (0–25) was complete at v1.21.0; `mmap` (27, **1.35.3**) + `munmap`
(28, **1.35.4**) — anonymous, 2 MB-granular — are the first new *functional*
syscalls since. They add a pure memory facility, not socket/crypto surface —
the attack-surface story stays anchored on the **absence** of AF_ALG / socket
/ `splice`, not on a fixed table size.

---

## Ecosystem (userland boot stack)

The kernel itself has zero deps (`[deps] stdlib = []` in `cyrius.cyml`).
What boots on top of it (live versions in [agnosticos `state.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/state.md)):

```
kybernet (PID 1)
├── agnosys      — syscall bindings (Linux x86_64 + aarch64 wrappers)
├── agnostik     — shared types/primitives (error/security/agent/telemetry)
├── argonaut     — service lifecycle, health, seccomp/Landlock, PID-1 harness
│                  (BOOT_MINIMAL mode adds agnoshi as no-deps console service — 1.30.x MVP path)
│   └── libro    — cryptographic audit chain
└── daimon       — agent orchestrator
```

**Single-pin convention retired**: the old "all on one cyrius pin" stack convention dissolved during the v5.11.x burst (2026-05-11/12/13); each repo now pins independently. **agnos is on cyrius 6.0.14** (6.0.1 → 6.0.3 at 1.35.5, 2026-05-27; held on 6.0.3 through the 1.37/1.38 big-write arcs; → 6.0.14 at the **1.39.0 cycle-open**, 2026-05-28, after a byte-identical kernel A/B — 6.0.3 and 6.0.14 emit the same `build/agnos` to the bit, all gates green on both; 6.0.1 had graduated from 5.11.64 mid-1.31.x). The per-repo pin-lag spectrum is tracked in [agnosticos `state.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/state.md) — the genesis repo is authoritative for cross-repo state. **Sibling versions intentionally elided here** to avoid double-bookkeeping.

---

## Test surface

| Gate | Count | Source |
|---|---|---|
| `scripts/check.sh` | **11/11** PASS | build, test, doc-exists ×6, version-in-kernel, version-in-changelog, binary-size |
| `scripts/test.sh --all` | **7/7** PASS | x86 builds, multiboot ELF, size, kernel_hello builds; aarch64 compiles, size, valid ELF |
| CI `boot-test` (QEMU) | banner + `KASLR: pmm_next_free=N` varies across 2 boots + `Memory isolation: PASS` + `Userland exec complete` | `.github/workflows/ci.yml` `boot-test` job |
| CI `Format check` | all kernel sources fmt-clean (1 skip: `kernel/user/shell.cyr` per `#ifdef`-in-fn-body carve-out) | `ci.yml` `check` job |

CI runs on a self-hosted runner labeled `[self-hosted, linux, x64]` for
`boot-test` and `benchmarks` (need QEMU + KVM-class CPU); `build`, `check`,
`test`, `security`, `docs` run on `ubuntu-latest`.

---

## In-flight (roadmap snapshot)

Source: [`docs/development/roadmap.md`](roadmap.md) `## Active` section.

| # | Item | Status |
|---|---|---|
| 1 | **1.35.x networking-comms — COMPLETE** | **1.35.1** TCP hardening B0–B4 (`tcp-smoke.sh` 4/4, `tcp-listen-smoke.sh` 2/2 — in-order ring + retransmit/RTO + MSS/segmentation + peer-window; reliable flow-controlled stream) + **1.35.2** NTP/SNTP (kernel's first wall clock; `ntp`/`date` verbs; `ntp-smoke.sh` parse PASS) — both **RELEASED**. The planned comms order DNS → ICMP → TCP-hardening → NTP is **done**. Audits: [`tcp-hardening-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/tcp-hardening-prior-art.md) + [`ntp-sntp-prior-art.md`](https://github.com/MacCracken/agnosticos/blob/main/docs/development/prior-art/ntp-sntp-prior-art.md). Next (cyrius-side, days-to-weeks): TLS + PIE. Legacy virtio-net back-burnered; plug-and-play a candidate. |
| 2 | AMD Zen Quiet-Boot scanout residue | Parked next-cycle pin (`project_amd_zen_scanout_residue`). MVP unblocked via VGA-spec path; resumption options are HUBP `clear_tiling` port or shadow-buffer FB-console architectural eval. |
| 3 | SMP AP wakeup on real hardware | QEMU-validated only; needs hardware-in-the-loop infra. Stays open across multiple arcs. |
| 4 | `struct Process` `#derive(accessors)` port | Was blocked on a cyrius cap-raise (16-field metadata-table overflow); revisit against the current cyrius 6.0.14 pin — picks up passively when the field-table cap is confirmed lifted. |

Recently closed — arc bookmarks only; per-cut detail in [`CHANGELOG.md`](../../CHANGELOG.md):
- **1.35.7 — arc-close hardening (pass 1)** (latest): `ip_safe_payload_len` clamps the IPv4 total-length at `net_poll` to the received frame — kills a forged-length over-read across ICMP/UDP/TCP. Non-structural (refactor → 1.36.x). `hardening-smoke.sh` green + icmp/tcp/dns/ntp no-regression. Pass-2 candidates in the audit doc.
- **1.35.6 — DNS cache**: 8-entry TTL-respecting positive cache (`dns_cache_find`/`_put`, lwIP-style evict-soonest) + TTL extraction; repeated lookups stop re-querying. `dns-smoke.sh` 3/3. (multi-A/CNAME + retransmit were already in the 1.35.0 stub.)
- **1.35.5 — RTC boot clock**: CMOS RTC read (`rtc_read_unix`) + `civil_to_unix` seed a local wall clock at boot (`date` `[RTC]`/`[NTP]`); NTP refines. Also moved the kernel cyrius pin 6.0.1 → 6.0.3 (byte-identical A/B). `rtc-smoke.sh` green.
- **1.35.4 — `munmap`**: syscall 28 + the inverse of mmap (PD-walk → `proc_unmap_page` + `invlpg` → `pmm_free_2mb`) + LIFO arena reclaim. Closes the mmap/munmap pair. `mmap-smoke.sh` 2/2.
- **1.35.3 — anonymous `mmap`**: syscall 27 + `pmm_alloc_2mb` 2 MB-contiguous allocator; 2 MB-granular zero-filled memory into the caller's address space. First new functional syscall since v1.21.0. (Was the roadmap `mmap (anonymous-only)` open item.)
- **1.35.0–1.35.2 — networking-comms arc**: DNS stub resolver + ICMP/ping + TCP hardening (B0–B4) + NTP/SNTP. The reliable-stream + name-resolution + wall-clock substrate for TLS. All RELEASED + smokes green.
- **1.34.x — FAT-family arc**: FAT12/16/32 + exFAT read **and write** (LFN, cluster allocator, root extension, spanning-append, overwrite/truncate/delete) + ESP-write safety guard (1.34.6). `fsck.fat` / `fsck.exfat` / `mtools` validated.
- **1.33.x — ext2/4 WRITE arc**: file create/write/truncate on ext2/ext4; persist-across-reboot iron-validated (Attempts 90/91).
- **1.32.x — networking arc**: TCP/UDP server primitives + DHCP client + r8169 NIC; iron-COMPLETE (DHCP lease verified 1.32.9; r8169 RX fix 1.32.7).
- **1.31.x — storage arc**: NVMe (iron Attempt 80) + GPT + AHCI/SATA (iron Attempt 87) + USB-MS + RAM-disk + 5-backend block layer + ext2/ext4 read.
- **1.30.x — MVP gate**: typeable shell on archaemenid at Attempt 68 (1.30.9) + FB-hardening sweep (true-font swap 1.30.12).
- **1.27.x – 1.29.x and earlier**: Path-C sovereign UEFI ABI (1.30.0), KASLR / Security Hardening 13/13 (1.28.x), VFS tagged-unions (1.29.x) — see archived `CHANGELOG.md` entries.

---

## Verification hosts

| Host | Purpose | Status |
|---|---|---|
| Self-hosted GH runner (`agnos-runner`) | CI boot-test + benchmarks on real KVM | Active |
| Dev box (Arch, Linux 7.0.3, QEMU 11.0) | Local builds, boot, bench | Active |
| QEMU `-cpu max` x86_64 | Required for boot (boot shim sets SMEP+SMAP in CR4 — `qemu64` default lacks both, triple-faults) | — |
| QEMU `-M virt -cpu cortex-a57` aarch64 | Build target; live boot not yet wired | Compile only |

---

## Refresh discipline

[`CHANGELOG.md`](../../CHANGELOG.md) carries the full per-cut narrative; this file is current-truth + pointers, **not a log**. Arc-level summary lives in the "Recently closed" block above.

`scripts/version-bump.sh` keeps the header date + Version-table row + `roadmap.md "Current"` line fresh atomically with the bump. Body prose (Build artifacts sizes, Source rollup counts, Subsystem status table, In-flight table, Recently-closed bookmarks) needs a **manual sweep at each minor closeout** — the script touches the header, not the body. This pattern was established during the 2026-05-18 doc-staleness audit (script-fresh header found next to v1.29.0-era body prose); the 1.35.0 cycle-open sweep (2026-05-26) brought the body forward from its frozen 1.31.1 shape through the 1.32.x networking / 1.33.x ext2-4-write / 1.34.x FAT-family arcs.
