# AGNOS Kernel Roadmap

<!-- TOOLING ANCHOR: scripts/version-bump.sh seds ONLY the version numbers in `> **Current**: vX.Y.Z`
     and `Built with cyrius X.Y.Z`. Delete either anchor and that sed becomes a silent no-op. -->

> **Current**: v1.56.51 — live state (kernel head, cyrius pin, active burn, sweeps, sizes) lives in [`state.md`](state.md).

Forward-only. Shipped work is not narrated here: history is [`../../CHANGELOG.md`](../../CHANGELOG.md), live state is [`state.md`](state.md), the normative syscall contract is [`agnos-userland-abi.md`](agnos-userland-abi.md). **All GPU/display/HDMI work is one open release (1.56.x) and lives entirely in [`planning/gpu.md`](planning/gpu.md)** — plan, register facts, falsified record, remaining ladder. Do not re-narrate it here and do not open a second GPU doc. Ship milestones (beta/GA/maturity arc) live in the agnosticos roadmap. Language roadmap: `../../../cyrius/docs/development/roadmap.md`.

---

## TRUE — measured constraints that bound every item below

- **Syscalls 0–100 are allocated; #100 `icmp_echo_ex` is the highest; the next free number is #101.** ⭐ **`#99` `proclist` (1.56.47) and `#100` `icmp_echo_ex` (a caller-chosen ICMP deadline, 1.56.48) are MINTED and QEMU-proven.** ⛔ **Widening a LIVE syscall's arity is NOT backward compatible on this ABI** — measured on cyrius 6.5.35, the compiler pops only as many registers as the call passes, so an unused argument register holds whatever the previous code left there, **not zero**. `#100` exists precisely because giving `#55 icmp_echo` a second `timeout_ms` argument would have handed every already-shipped one-argument caller a garbage bound. **Mint a number; do not widen an arm.** ⭐ **`#97` `chan_op` (the local-IPC channel band, 1.56.40) and `#98` `ptrscan` (the pointer band, 1.56.42) are MINTED and iron-proven** — arms at `syscall.cyr:7922` and `:8786`. **`#96` = `fork` remains RESERVED and unminted** ([ticket](issues/2026-08-05-syscall-96-fork.md)); the number is not available to anything else. ⛔ **Do not mint a constant before its kernel arm exists** — an unknown `num` falls through the dispatch chain and the caller reads the fall-through value as data.
  ⚠ **This line said "0–95 allocated, #95 highest, next free #98, neither minted" until 2026-08-11 — three of its four clauses were false.** ⭐ The ABI table one file over was CORRECT the whole time, because `syscall-abi-check.sh` fails on a row with no kernel arm and runs in `check.sh`. **The same fact lived in two places and only one was gated.** Prefer pointing at [`agnos-userland-abi.md`](agnos-userland-abi.md) over restating it here.
- ✅ **The ABI table is COMPLETE and machine-checked as of 2026-08-05** — it documented **64 of 96**; 32 rows were backfilled and `#84` corrected (`present` → `gpu_present`). Kept that way by **`scripts/check/syscall-abi-check.sh`** in `check.sh`, which diffs the kernel dispatch, this table and the cyrius `SysNrAgnos` peer, and fails on a missing row, a name disagreement, a row with no kernel arm, or a syscall row filed in a struct section. ⭐ cyrius was never the gap (96/96 constants + wrappers). → [ticket](issues/2026-08-05-abi-doc-covers-two-thirds-of-the-syscalls.md)
- **User memory is 2 MB huge pages only.** No 4 KB user PT level exists; `proc_map_page` writes a PD entry as `phys | 0x87`. `mmap` is therefore 2 MB-granular — `mmap(4096)` returns 2 MB. Low arena [0x10000000, 0x40000000); high arena [0x2000000000, 0x8000000000) via per-process PDPT[128..511].
- **Every process on a CPU enters syscalls on ONE shared per-CPU stack** — `pcpu_syscall_kstack_top = 0xF10000 + cpu*0x10000` (`syscall_hw.cyr`). Only the *interrupt* entry stack went per-process (1.46.1). This is why `preempt_disable()` guards every blocking sti-window and why `sched_yield`#44 abandons its frame.
- **Exec init stack: `rsp` starts at `stack_base + 0x3000` with `stack_base = 0x3FC00000`** — ~12 KB of usable downward stack out of a mapped 2 MB page. `iam`'s `main` carried ~17.6 KB of stack buffers, hard-faulted at CPL3, and never rendered (worked around app-side in iam 1.1.5).
- **16-process cap** (`proc.cyr:278`, `if (proc_count >= 16)`). ⭐ **REACHED IN PRACTICE 2026-08-09, not just in theory**: the desktop leaked ~3 procs per relaunch (a compositor that exits without closing its clients — nothing reaps them) and launch #4 found the table full. The refusal was **silent on both sides** until 1.56.42 made `proc_alloc_slot` name it. ⚠ The line reference here read `:176` until 2026-08-11 and pointed at `fn fpu_area`. `#37`/`#43` cap path and argv at **127 bytes** and env at **1024 bytes across 16 entries** (the argv *token* cap was raised 8→16 at 1.46.10).
- **Net:** 8 concurrent connections, 8 listeners; TCP RX ring 2048 B; `sock_recv`#49 returns **0 = WOULD_BLOCK and −1 = EOF**, inverted from Linux `EAGAIN`. `epoll` watches only `VFS_SIGNALFD` and `VFS_TIMERFD`, never sockets, and is 3-arg (no event struct, no per-fd data, no timeout).
- **Audio is playback-only and format-fixed.** `snd_config` accepts 44100 or 48000 Hz, 16-bit, stereo; resample lives in the producer, which binds every future audio consumer. `snd_open`#64 takes no direction argument (a1..a4 unused) and arms exactly one output stream off `hda_out_dac`.
- **One 2 MB PMM bitmap covers 16,777,216 pages ⇒ a 64 GB RAM ceiling.** Beyond it RAM is capped and logged, never silently dropped.
- **`initrd_init` is fed a fixed synthetic image** — `main.cyr:4978-4979` calls `initrd_build_test(0x6000)` then `initrd_init(0x6000)`; aarch64 uses 0x40700000. Nothing reads `boot_info`.
- **Standing boundaries (not work items, do not expire):** the in-kernel emergency shell is permanent, not transitional; kernel growth is per native workload — no POSIX `socket()` emulation, no foreign-ABI absorption; `fb_phys` is never exposed; **CWD is userland-owned — no `chdir`/`getcwd` syscall, and one is not to be added** (ABI §3.2, the 1.41.3 decision); `kriya` is the standalone `/bin` coreutils set and supersedes agnoshi's in-process FS builtins; agnsh AI/`hoosh` wiring is its own later arc; **cyrius is hands-off** — surface bugs, never edit that repo. Not in the queue at any horizon: Apple Silicon as a kernel-on-iron target (Asahi owns it), Landlock/seccomp/`unshare` ABI emulation, the GRUB/multiboot2 boot path (dead under strict W^X; gnoboot is the sovereign `BOOTX64.EFI`).

## FALSIFIED — closed or reverted; do not re-open without new evidence

- **"Only two execs per boot" — CLOSED.** Each `run` had permanently consumed 2 of the 6 usable 2 MB regions (code + stack), so the 4th run hit `pmm_alloc_2mb` exhaustion and printed `run: not an executable`. `proc_reap` / `proc_reap_child` now free the address space and all user pages on exit.
- **"A malformed on-disk ELF leaks page tables and mapped pages" — CLOSED at 1.41.6.** A PT_LOAD pre-pass validates every segment *before* `proc_create_address_space`, so a bad ELF returns with zero allocations to leak.
- **Multi-slot `proc_count` collapse ("while trailing-dead") — TRIED at 1.44.15 and REVERTED.** It reclaimed the slot of an exited-but-not-yet-`waitpid`'d proc, so the parent's `waitpid(pid)` found `pid >= proc_count` → −1 and that proc's address space leaked. A slot must persist until its OWN waitpid reaps it.
- **"`reboot`#13 is a stub that calls `arch_halt()`" — STALE.** #13 runs the real `power_sys` ACPI S5 / CF9 shutdown path, gated on the magic pair so the garbage-argument callers the old nullary ABI left behind fail closed. The missing update primitive is the boot slot, not the reboot.
- **"2D acceleration lands on the GFX ring" — WRONG.** It shipped as CP-DMA `DMA_DATA` on the MEC compute ring. SDMA was abandoned after six burns: the ring comes up first try and the F32 runs, but the engine never fetches (`cap 0` = the doorbell was never CAPTURED) — an agnos setup gap, **not** a hardware limit; Linux drives SDMA on this box. Resume state: `rptr 0 wptr 44 cnt 266269 base 4102029824 halt 0 cap 0 cntl 262146`.
- **Hardware-validation infra lost its only stated justification.** It existed to unblock SMP-AP-wake on real hardware; that shipped without it, validated by direct archaemenid burns in the 1.46.x arc. Needs a fresh rationale or re-siting as a 1.6x prerequisite.
- **Decade-map collision — RESOLVED 2026-08-11 by operator call.** The map assigned 1.5x to Intel while the AMD line consumed it (now 1.56.x). ⇒ **Intel is slotted at `1.58.x`**, a concrete minor rather than a decade, immediately after the current AMD work — *"we can attack it then as we are really close to most of the amd side of things now"*, with the **i225-V ethernet driver** as its first deliverable. ⚠ The other platform lines are unchanged and still fit ahead of it: Pi/aarch64 **1.6x**, radios **1.7x**, RISC-V **1.8x**. ⭐ The lesson the collision leaves behind: a decade-per-platform scheme breaks the moment one platform outgrows its decade, so **a platform may be slotted at a MINOR**, not only a decade.

## OPEN

`unslotted` = no release assigned; confirm scope with the operator before opening any of them.

| Item | Why it is needed | Target | Blocked on |
|---|---|---|---|
| ⛔ **The carveout layout is STATIC HEX TUNED TO A 3 GB APERTURE — agnos fails or wastes on any other GPU** | Every region is a fixed offset in `gpu_regs.cyr` sized for archaemenid's **3072 MB** UMA carve, and the TD-3 audit asserts `rt_end == carve_bytes` **exactly** (`gpu.cyr:773`) — *"ending at the top is the whole reason an overrun is survivable, because it runs off into the fault net"*. `GPU_RT_REGION_OFF + GPU_RT_REGION_SIZE` = `0xB0000000 + 0x10000000` = `0xC0000000` = 3 GB, which equals `RCC_CONFIG_MEMSIZE` on this box and **nothing else**. ⇒ On a discrete card with 4/8/16 GB the audit FAILS, `gpu_rt_audited` stays 0 and `gpu_rt_handle_mc` refuses — the render-target region is dead on exactly the hardware with the most VRAM. Below 3 GB it fails too. ⭐ **This is the parent item for 5K/8K**: the aperture is a per-DEVICE fact read from the hardware (`gpu.cyr:742`), not an agnos cap, and a mobile APU's UMA carve is the low end of the range, not the ceiling. The regions should be DERIVED from the measured `vram_mb` — anchor the RT region at the top, place shm beneath it, and let the slot size scale — so a bigger aperture yields bigger slots instead of a failed audit and 80%+ waste. ⚠ `scripts/check/check-carveout.sh` gates the constants statically; a derived layout needs the same check at run time against the measured size. | unslotted | — (self-contained; wants a second GPU to validate against, see below) |
| **5K / 8K displays — reopen when a higher-end GPU is on the bench** | 5120x2880x4 = **58,982,400 B** (wants 64 MB slots); 7680x4320x4 = **132,710,400 B** (wants 128 MB, which fits with 1,507,328 B spare). Neither fits the 32 MB slot 1.56.44 ships, so both fall back to the CPU path — and in aethersafha that demotes the whole frame, since `AE-9` left no CPU layer to mix into a GPU frame. ⚠ **On archaemenid these are blocked by the 3 GB UMA carve, which is a MOBILE APU's limit and not agnos's.** 16 x 64 MB = 1 GB and 16 x 128 MB = 2 GB; the carveout is 80.7% unclaimed but scattered, so on THIS box 5K needs a compaction and 8K leaves no slack anywhere. On a discrete card with GBs of dedicated VRAM both are comfortable — which is why the derived-layout item above comes first: fix the layout to scale with the aperture, then these open by arithmetic rather than by re-partitioning. ⚠ Budget the rest too: at 8K the console FB is ~133 MB, pan ~265 MB and back buffers ~265 MB. ⛔ Do NOT reach for a variable-extent allocator: uniform stride is what makes `shm_free` a table clear with no release path and keeps `#89 +20 freeslots` meaningful (index IS address, `syscall.cyr:1003`). ⚠ Slot COUNT vs slot SIZE is a policy call — how many concurrent GPU surfaces a desktop may hold — and wants an operator ruling before anything is built. | unslotted | the derived-layout item above; and a test device with a higher-end GPU |
| ⭐ **Sovereign shader pipeline — 2 of 21 shaders have an emit list; the remaining 19 are the work** | 1.56.44 landed the mechanism AND its first consumer-driven shader: `blend_rect` (60 dwords) and `blend_alpha` (69) both reproduce their committed hex byte-for-byte through mabda's encoder, gated by `tests/gpu/shaderasm.cyr` against tables generated from `gpu.cyr` by `scripts/check/shader-tables.sh`. ⭐ **`blend_alpha` was BURNED 2026-08-16** — `#92` op **0x06** (not `0x11`; that code sits in the reserved CP-DMA lane and this is a shader dispatch), `#89 +28` = `0x1FF5F`, a translucent client surface composited over 244 and 537 frames. ⚠ Its ROUNDING TIE is still unproven: 5,905 reachable ties, RTNE vs half-away differ on 3,010 outputs, and no visible desktop discriminates ±1 on a channel. ⛔ **Coverage is the honest problem: 19 shaders still have no sovereign derivation, and 13 of 20 blobs are gated by NOTHING at all** — `check.sh:122` covers 7 by llvm-mc, `edgeasm` covers 4, and the two sets barely overlap. `blend_premul`, `glyph_1bpp`, `grad_linear`, `perm`, `blend_pk`, `tri_depth`, `tri_persp` and the four `matmul_*` are unverified against anything. **Ordered remainder:** (a) re-anchor `edgeasm`'s hand-copied `bc_want`/`want` onto the generated tables and delete its duplicated wrapper layer, now that `asmlib.cyr` exists; (b) SGPR high-water tracker + sovereign RSRC1 derivation, calibrated against every shipped `GPU_COMPUTE_PGM_RSRC1_*` — ⚠ this replaces a value harvested from an assembler descriptor with one computed by a formula, and `shader-blob.sh:44-46` records that exact derivation being wrong once, with **no hardware oracle** to contradict it; (c) ✅ **DONE 1.56.44** — `blend_alpha` (+9 dwords over `blend_rect`, `#92` op **0x06**) for aethersafha M6-C3 per-window opacity, burned and consuming; (d) the mabda 4.1.0 PR below. ⚠ **VOP3P and SOPK are missing from mabda**, which permanently blocks emit lists for `blend_pk` and the three `matmul_*` until it lands them. | unslotted | — (self-contained) |
| ⛔ **FALSIFIED 2026-08-13 — there is NO mabda bug. Retained so it is not re-derived.** The claim was that `mabda/src/gfx9_abi.cyr`'s `sgprs = (sgpr_hw + 2 + 7) / 8` under-allocates, because gfx90c "grants `roundup8(nfs + 6)`, the +6 being VCC(2) + XNACK(4)". **Both halves are wrong, and the error was agnos marking mabda's homework with agnos's own answer key.** `llvm-mc` is not an oracle for the *correct* SGPR grant — it emits whichever reservation policy you declare. Solved uniquely over `nfs = 1..39` on gfx90c (llvm-mc 22.1.8): bare defaults **E=6**; `xnack-` alone **E=6, unchanged**; `.amdhsa_reserve_flat_scratch 0` alone **E=4**; both waived **E=2** — mabda's exact value. ⇒ agnos's `.s` files ask for LLVM's conservative default; mabda implements `xnack-` + no-flat-scratch. Both are arithmetically correct for their policy. ⚠ **XNACK contributes 2, not 4, and is INVISIBLE until flat scratch is waived** (they overlap at the top of the file), so for agnos's configuration the dominating term is FLAT_SCRATCH and XNACK is not the discriminator at all — even though agnos's kernels use no scratch (no `scratch_`/`flat_` instruction in any shader, every shipped RSRC2 has bit 0 clear, and `gpu.cyr:3011` records `COMPUTE_TMPRING_SIZE` has never been written). ⇒ **agnos over-declares by one 8-SGPR granule on 5 of 20 shaders** — `blend_pk`, `blend_rect`, `grad_linear`, `glyph_1bpp`, `edge_cov` — which costs occupancy and nothing else. **Recommendation: change nothing.** ⚠ Two agnos DOC defects do need fixing, because they are load-bearing and false: the "+6 = VCC(2) + XNACK(4)" rationale in `gpu_regs.cyr:1375`, `planning/gpu.md:902`, `planning/rung17-tri-depth.md:375`, `tri_persp.s:122` and `tri_depth.s:108`; and `gpu_regs.cyr:1373` calling `tri_persp` "56 SGPRs" when `0x002C01C7` is field 7 = 64. | n/a — closed | — |
| **`sys_mmap` low arena — global cursor, unlocked** | `proc.cyr:1385` `var mmap_next_vaddr` is ONE global for all procs, read-modify-written unlocked inside the mapping loop while the returned base was captured before it. Two concurrent `npages > 1` mmaps punch a hole: a caller is handed `base, len` with a middle page never mapped in its CR3 → `#PF` → `exit 142`. The HIGH arena three lines below already reserves per-process (`himmap_reserve`, 1.50.4) — same bug class, fixed one arena over. Trigger is live: `aethersafha/src/setu_dispatch.cyr:57-58` allows 16 MB allocs that spill to `sys_mmap`. ⚠ **NOT** the 2026-08-02 large-image fault (that PDE is written-then-zeroed; this one is never written, and the arena starts at PD[128] while those victims are PD[510]/PD[9]). → [`issues/2026-08-03-mmap-low-arena-global-cursor-smp-hole.md`](issues/2026-08-03-mmap-low-arena-global-cursor-smp-hole.md) | unslotted | — (fix is self-contained: lock the cursor, or reserve the span atomically like the high arena) |
| **Raw `syscall(N,…)` with LINUX numbers on agnos paths** | agnos redefines the syscall enum, so a raw literal compiles clean and calls a different arm. Confirmed shipping in jalwa: `mkdir`(83)→GPU f64 dispatch, `poll`(7)→`open` **per frame** on the GUI path, `read`(0)→**`exit`** in `mcp_serve`. Plus a latent one in **agnostik** `_fill_random`, where `syscall(2,…)`→getpid returns positive so the `fd >= 0` guard passes and the next line `syscall(0,…)`→**exit** kills the proc — every agnostik ID generator funnels there. → [`jalwa/docs/development/issues/2026-08-03-agnos-raw-syscall-wrong-numbers.md`](https://github.com/MacCracken/jalwa/blob/main/docs/development/issues/2026-08-03-agnos-raw-syscall-wrong-numbers.md) | unslotted | fixes live in jalwa + agnostik (tag bump to propagate); kernel side is unaffected |
| ⭐ **IPv6 — the network stack is v4-ONLY, end to end** | ⛔ **agnos has no IPv6 at all.** `grep -rliE 'ipv6|0x86DD' kernel/` returns **nothing**: no `0x86DD` ethertype arm in the eth demux, no 128-bit address type, no NDP, no ICMPv6, no v6 routing, no DHCPv6/SLAAC. This is a **subsystem, not a syscall** — which is why it is filed here rather than as a number to mint. **What it blocks, concretely:** `yo -6` cannot work on AGNOS (yo's `platform_icmp6_*` return `-1` by design, so the family reports unavailable rather than failing obscurely); `%zone` scope IDs are meaningless without link-local v6, which is why an ifindex lookup is **not** separately backlogged — on a single-NIC box it would be a `return 1` with nothing to scope; and `dig`/`whirl` are v4-only on agnos for the same reason. ⚠ **Ordering, if this is ever opened**: ethertype demux + a 128-bit address type first, then **NDP — v6 has no ARP, neighbour discovery IS ICMPv6, so ICMPv6 is a PREREQUISITE of basic v6 reachability, not a feature layered on top** — then ICMPv6 echo, then a v6 arm on `icmp_echo_ex`#100 or a sibling number. The focused-syscall posture ([yo ADR 0002](https://github.com/MacCracken/yo/blob/main/docs/adr/0002-focused-kernel-icmp-syscall.md)) says that ring-3 surface should stay `icmp_echo`-shaped rather than growing a sockets API to accommodate it. ⭐ **Not urgent by evidence**: every iron burn and QEMU smoke to date ran on v4-only networks and no consumer has asked. Filed so the gap is a decision rather than a discovery. | unslotted | — (self-contained; large) |
| ⛔ **1.56.51 P-1 audit backlog — PART-WORKED 2026-08-29: P0 1/2, P1 17/26, P2 0/29** | The 1.56.51 sweep ran 12 subsystem auditors and adversarially verified every finding; 79 of 101 survived, and the ones that did not land in that cut are enumerated with mechanism + failure trace in [`issues/2026-08-28-p1-audit-sweep-backlog.md`](issues/2026-08-28-p1-audit-sweep-backlog.md). ⭐ **The top item is `elf.cyr:312`** — a PT_LOAD may be mapped anywhere in the per-process kernel-identity PD window, and the issue doc carries a full ring-3 → arbitrary-kernel-write chain through it (shadow the child's own PML4/PDPT/PD, then let `sys_mmap`'s `load64(cr3)` read attacker bytes). ✅ **CLOSED 2026-08-29** by moving the kernel's dereferences off identity VAs — the remedy already applied to the PMM bitmap and TSS RSP0, now generalised to every page-table walk (`ptw_pd_kva`), `proc_create/free_address_space`, `proc_copy_from/to_user`, `sys_munmap`, the fault-handler forensics walk and `kmalloc`. The 1.56.51 cut had only DOCUMENTED the hazard at the ceiling (largest real binary reaches 20.99 MB against a 256 MB ceiling); narrowing that bound shrinks the window without closing the class, which is why it was not the fix. ⚠ **Treat every entry as a LEAD**: the sweep produced real false positives, all of one shape — an auditor reading `var x[N]` as N bytes when it is N u64 slots. ⛔ Nine verifiers were lost to a spend limit in the klib/cross-arch area, so those findings have no refuter behind them. | unslotted | — (the `elf.cyr:312` ruling was GIVEN 2026-08-29: close the class properly. Done — every kernel page-table deref and every `kmalloc` pointer now goes through `DIRECTMAP_BASE + phys`. ⛔ The remaining P0 is `#92`'s TOCTOU at `syscall.cyr:1895`; the 9 remaining P1s and all 29 P2s are marked inline in the issue doc.) |
| ⛔ **aarch64 does not compile** | 30 reachable undefined functions + 18 undefined variables; `arch/aarch64/stubs.cyr` has not kept up with `core/`. It went unseen because `scripts/test.sh`'s cross-compiler probe named `cc5_aarch64`, a binary dropped at cyrius v6.1.0 — so the aarch64 half of `--all` early-returned scoring neither a pass nor a fail, on every run since. Probe and silent-skip fixed 1.56.51; `test.sh --all` is now honestly RED. ⚠ Two sites were also still using `proc_current`, the global removed at 1.46.x with the stated intent that "any unconverted read fails to COMPILE" — it did, into a build nobody ran. Converted at 1.56.51. The rest of the surface is untouched. | unslotted | an operator decision on whether the 1.6x aarch64 arc opens now or later |
| **`fork`** (**reserved `#96`**, operator-assigned 2026-08-05) | `agora`'s fork-per-connection BBS cannot run on agnos: `agora/src/main.cyr:2702` calls `sys_fork()` and no fork syscall exists. Full-copy (not CoW) `proc_dup_address_space` over PD[1..510]. | unslotted | needs `waitpid` wait-any first (it forces the `proc_ppid[16]` array fork also needs) |
| **HID halted-endpoint recovery is unvalidated** | The re-arm and ISR-console-lock halves of [`issues/2026-08-11-hid-drain-rearm-and-isr-console-lock.md`](issues/2026-08-11-hid-drain-rearm-and-isr-console-lock.md) are **fixed and measured** (fault injection + a control that fails; mutation-tested). What remains is `hid_recover_halted()`: reachable and harmless, but its Reset Endpoint / Set TR Dequeue pair **has never executed**, because an injected completion code is fabricated in software — the controller never actually halts, so `xhci_ep_state()` reports Running and the body early-outs. ⇒ Needs a genuine hardware stall to validate; the sequence mirrors `msc.cyr`, which is iron-proven, so it is prior-art-shaped rather than speculative. ⚠ Also from that investigation — the `klug.cyr:19-21` falsehood (that the kernel stops feeding the ring at the userland handoff) is **fixed 1.56.48**; `devs.cyr:71` feeds it for every console-bound ring-3 byte, so **printing the log still destroys the log**, unfixed by decision with both seams named in the 1.56.48 CHANGELOG. Still open: MSI-X is armed one call before the last EP0 control transfers (`main.cyr:542` vs `:553`, boot-window only — NVMe takes `blk_active`), and `hid_mouse_seq` advertises a ring-3 capability that is in no syscall. | unslotted | a real stall, or controller-level fault injection agnos does not have |
| **`VFS_CHAN` close leak** (carried from 1.56.41) | `vfs_close_inner` has arms for `VFS_EXT2_FILE`, `VFS_SEC_WFILE`, `VFS_PIPE` and `VFS_SOCK` — and **none for `VFS_CHAN`** — so closing a channel fd zeroes the slot and leaves the **endpoint claimed until the process dies**. 32 endpoints = 16 channels, so a long-lived server that opens and closes channels exhausts them. ⚠ **This was carried debt with no roadmap row until 2026-08-11** — it lived only in `state.md`, which is the volatile file, so it would have aged out. ⛔ **Whoever takes it must re-derive authority first**: an inherited, non-owned channel fd in a child must NOT release its parent's endpoint on `close()` — that is the violation the band exists to prevent. `chan_auth` already implements the rule and `VFS_PIPE`'s owner-aware release is the shape to copy. ⚠ `vfs.cyr` is included ahead of the chan state, so the fix belongs at the `SYS_CLOSE` dispatch, **not** in `vfs_close_inner`. | unslotted | — (self-contained) |
| **`waitpid`#4 wait-any** | #4 implements WNOHANG for a *single* pid only; `arg1 < 0` (scan for `ppid == proc_current && state == 0`) is missing, and every fork-model server needs it. | unslotted | — |
| **`proc_reap` non-top-slot residual** | Both reaps collapse `proc_count` only when `pid == proc_count - 1`; a non-top exit holds its slot until the slots above drain, raising the `proc_count` high-water against the 16-slot table. Pages are **not** leaked (address space freed regardless) and `proc_alloc_slot` (1.44.12) reuses the hole — this is a table-pressure residual, not a leak. | unslotted | a real multi-proc workload showing slot pressure; the naive multi-collapse is falsified above |
| **Nested/recursive exec — `execwait`#37 re-entry** | The per-CPU `pcpu_ew37_busy_get() != 0` guard returns −1 ("out of scope until the multithreading arc"). takumi is itself a spawned ELF and must exec N build children, so today it must drive `spawn_path`#43 + a polling `waitpid`#4 loop. | ark M5/M6 | — |
| **argv/env length-cap raise** | 127-byte path+argv and 1024-byte/16-entry env are both exceeded by real build invocations. Pairs with nested exec. | ark M5/M6 | — |
| **Atomic system-update / boot-slot primitive** (ark M4) | The update mechanism the maturity arc names. Absent in gnoboot (its "slots" are `boot_info` struct fields) and absent in agnos. **A design call is owed first:** A/B image swap selected by gnoboot, versus an ark-driven in-place re-materialize from a verified `.ark`. | ark M4 | the design call |
| **SMP TX/conn-side lock residual** | `net_rx_drain` is cross-CPU-safe via the atomic `net_rx_trylock` xchg (1.46.x STEP-2 #5), but the TX/conn mutators — `nic_send` / `tcp_send` / `tcp_connect` touching `tcp_conns` and `arp_cache` — are still unlocked. Only reachable once a net program runs on an AP; today the net stack is BSP-serial. Needs short-held conn-table locking that never spans the blocking ~8 s `tcp_connect` poll (which would starve the BSP RX drain) and never self-deadlocks the RX-dispatch→ACK-send path. | unslotted | a net program scheduled on an AP |
| **Path 2 — per-process syscall kernel stacks** | Lifts the shared-kstack invariant above; plan doc [`planning/blocking-syscall-concurrency.md`](planning/blocking-syscall-concurrency.md). Path 1 (userland cooperative `sched_yield`#44) shipped and is QEMU-validated, so this is deferred, not blocked. | unslotted | a workload needing truly-blocking syscalls *and* concurrency that cooperative yield cannot express |
| **Mic / audio capture** | The band is output-only: `snd_open`#64 has no direction argument and arms one output stream. HDA input stream descriptors are enumerated at probe (`hda_iss` from GCAP; ISS=4 on archaemenid) and never armed. ⚠ USB-side capture is a different and larger problem — UAC mandates isochronous, which agnos does not have. | unslotted | a capture consumer |
| **`initrd` read from `boot_info`** | gnoboot 0.5.0 loads `\boot\initramfs` and fills `initramfs_phys`@0x10 / `initramfs_size`@0x18, but the kernel still mounts the synthetic image at the fixed address (see TRUE). Blocks the read-only live `.iso` path (N1b). | unslotted | — |
| **AGNOS-native binaries cannot read their environment inside a container** | `getenv` on the LINUX target reads **`/proc/self/environ`** (`cyrius .../lib/io.cyr:779-780`: `file_open("/proc/self/environ", 0, 0)`; `fd < 0 -> return 0`), not the stack `envp`. The agnos target is unaffected — the `#ifdef CYRIUS_TARGET_AGNOS` arm delegates to `_agnos_getenv`, which reads the r15-parked init rsp per ABI §4.6. ⇒ Any cyrius-built binary run as a **container payload on Linux** returns 0 from every `getenv` unless the runtime mounted `/proc`, **silently — no error, no log line**. glibc and busybox payloads are unaffected because they read the real `envp`; this hits AGNOS-native binaries specifically, i.e. **daimon and sutra**, which are exactly the intended container workloads. ⚠ Surfaced 2026-08-21 while fixing stiva's env-delivery bug (stiva 3.0.18 + kavach 3.12.0 `config_env`): the environment now reaches the payload's `envp` correctly and an AGNOS-native payload still sees nothing, so the stiva fix is incomplete for our own consumers. ⛔ Two candidate fixes and they are NOT equivalent — **a design call is owed**: (a) **cyrius-side** — have the Linux `getenv` prefer the stack `envp` and fall back to `/proc`, which fixes every consumer at once and is where the asymmetry actually lives (language roadmap, not this one); or (b) **runtime-side** — stiva mounts `/proc` in the container, which is correct OCI behaviour regardless and is tracked there as the write-only `RuntimeSpec.mounts` field. (b) does not remove the trap for any other runtime; (a) does. ⚠ Do not assume (b) alone closes this. | unslotted | the (a)/(b) design call; (a) is a cyrius change, (b) is stiva's |
| **Bigger usable user stack** | ~12 KB of real stack per program (see TRUE). Fix: put the init-stack layout (argc/argv/envp/auxv) near the *top* of the already-mapped 2 MB page and set `rsp` there — ~2 MB of downward stack, no new mapping. Low risk but it touches every program's entry layout. | unslotted | validate against the full `exec-smoke` |
| **Socket-as-VFS-fd bridge** | Landed 1.49.4, partially reverted 1.53.9: `sock_accept`#57 returns the raw `conn_id` again because cyrius `net.cyr` treats the return value as one. Accepted sockets are therefore not epoll-able; `vfs_create_sock` exists with no consumer. ⚠ `syscall.cyr:2431`'s comment still describes the reverted behaviour — fix in passing. | unslotted | folds into a broader VFS-fd unification; slot with it |
| **`lstat`** | The only half of the old pair left — `readlink`#70 landed at 1.53.6 with its cyrius peer. | unslotted | a consumer (kriya `ln -s`, or ark install layouts) |
| **USB class dispatch → hot-plug → hub → isochronous** | Verified at 1.56.23: `xhci.cyr:1235` hardcodes exactly one match (`class 0x03 / sub 0x01 / proto 0x01`); everything else is dropped. `xhci_drain_port_change_events` *discards* port-change events, so attach-after-boot never enumerates. No hub driver (root-hub ports only). `grep -riE 'isoch\|isoc' kernel/` returns **nothing** — no isochronous TRB form, no microframe scheduling, no bandwidth reservation; UAC and UVC both mandate it. Sequence in that order: dispatch is the cheap refactor everything registers against, hot-plug self-justifies (it fixes the HP external Blu-ray derping archaemenid at cold boot), isochronous is last because nothing queued consumes it. ⚠ The **mouse** is not here and never was — it needed only interrupt-IN, which agnos already configures, and it **SHIPPED iron-proven at 1.56.42** (`hid: boot-mouse interfaces bound: 2`, including the composite same-slot case). Do not read the mouse as evidence that class dispatch is partly done: it is exactly the hardcoded single match this row exists to replace. | decade TBD — plausibly a 1.7x prerequisite | operator confirmation on decade placement |
| **Optical via USB-MS (SCSI MMC) or ATAPI** | `msc_print_pdt_label` decodes optical PDTs but Phase 4 does not differentiate by PDT, and `msc_read_demo` skips silently when the sector size is not 512; AHCI skips ATAPI ports. | with USB classes | descriptor-driven class dispatch (PDT differentiation is downstream of it) |
| **HTTPS client, then `ark` package-fetch over HTTPS** | The sovereign package chain's network leg; the network-tools family tail rides with it. | unslotted | — |
| **Server-app tier on agnos** (`agora`, `descent`) | Proves the *server* maturity stage on the 1.32.x networking surface. `descent` is epoll-adaptable; `agora` is architecturally fork-blocked. | unslotted | `fork` + wait-any |
| **`ai-hwaccel` agnos compatibility** | Blocks `mihi`/`iam`/`chakshu` GPU-probe sysinfo; the probe pulls `thread`, `atomic` and Linux `CLONE_VM`. The stated precondition ("once agnos has threads") has fired — 1.44.x closed and AP scheduling is live. Userland, not kernel. | unslotted | — |
| **`sit` → agnos port** (unblocks `owl` 1.4.x's VCS gutter) | `owl --agnos` fails at the `sit` dep because `sit` calls `syscall(SYS_CHDIR, …)`. The first blocker is a **design collision, not a missing syscall** — CWD is userland-owned, so the port must make `sit`'s object store CWD-free and must **not** add a kernel `chdir`. | unslotted | userland only |
| **`tcsetattr`/`ioctl` no-op stub** | May be needed by darshana-class TTY tools. Distinct from `winsize`#60, which shipped — do not conflate. | unslotted | a consumer |
| **Multi-region contiguous bitmap >64 GB** | Lifts the 64 GB ceiling in TRUE. Fine for the 60 GB box; a real wall beyond it. | unslotted | hardware that exceeds it |
| **`mmap` follow-ons** | 4 KB-granular / partial mapping (needs a 4 KB user paging level — a large VMM arc), file-backed mapping (needs a VFS page cache), and a vaddr free-list for non-top arena holes. Neither prerequisite exists in the tree. | unslotted | a consumer showing real fragmentation or sub-2 MB need |
| **Userland >1 GB mmap program** | The 1.50.2 arena lift is iron-validated only via `MMAP_HIMEM_E2E_SELFTEST` (1.026 GB contiguous map-and-free), which drives the mechanism directly — the live ring-3 `sys_mmap`/`sys_munmap` path above 1 GB has no consumer. The per-process cursor (1.50.4) and high-range `munmap` (1.50.3) are already in place. Trigger has fired: the desktop is live on agnos. | unslotted | — |
| **MADT AP enumeration** | `smp.cyr` hardcodes an AP count of 4; no MADT parsing exists anywhere in the kernel. Deferred at the 1.44.22 audit as not worth perturbing iron-validated code; the 1.46.x SMP arc opened and closed without touching it. | unslotted | — |
| **Loader DRY** | `spawn_user_proc`/`elf_load` and `elf_load_from_file` still carry near-duplicate stack-mapping logic. A refactor inside the iron-validated exec path. | unslotted | — |
| **Native sandbox-confinement primitives** | `kavach`'s `security.cyr` returns `err_not_supported` for LANDLOCK / SECCOMP / NAMESPACES on agnos, so sandboxes have capability gates and no enforcement. Build **capability-scoped, kernel-enforced** FS reach, syscall reach and resource budget — explicitly **not** Landlock/seccomp/`unshare` emulation. **Converges with the ark build-confinement item — build it once.** Adjacent cyrius prerequisite (filed): the stdlib `O_*` open flags are undefined for the agnos target and the values differ (`O_CREAT` 64 vs `AO_CREAT` 0x100; `O_EXCL` has no `AO_*` peer), so a naive Linux-value define is wrong. | unslotted | a workload needing enforced confinement |
| **NTFS read · squashfs read** | Windows-volume interop; and a compressed read-only FS leveraging `sankoch` in-kernel. Both read-only, each its own minor. Neither exists in the tree. | unslotted | — |
| **HTREE indexed directories (ext4)** | A linear dirent scan suffices for read and `ext2.cyr` never creates indexed dirs; HTREE is a 10k+-entry performance optimization. | unslotted | a real consumer |
| **Perf tail** | Deferred *with reason*, and the reason is the value. **Syscall entry/exit:** the `ksyscall` chain already puts the hot calls (`exit`, `write`, `getpid`, `read`, `close`) at depths 1–7, so a reorder is near-zero win and the entry trampoline is hand-asm. **Context switch:** per-tick save/restore is hand-asm; the 1.44.22 audit called it low-headroom, higher-risk. Also open: block-IO batching; exFAT bitmap scan (dominated by per-byte bit counting, not I/O); fatfs multi-cluster write coalescing (structurally blocked — `fat_alloc_cluster` allocates one at a time so clusters are not contiguous; needs a contiguous-allocation strategy first, which exFAT already has). | unslotted | a profile showing any of them is actually hot |
| **Radios — WiFi + USB Bluetooth** | A multi-cycle lift, deliberately late so it inherits a proven IP/TCP/DHCP stack, matured xHCI bulk/interrupt machinery, matured `sigil` crypto and the post-2.0 kernel. WiFi needs firmware + an MLME + a WPA2/3 supplicant; Bluetooth needs HCI-over-USB, then L2CAP, then RFCOMM/GATT. Nothing exists yet — correctly, no `core/radios/` directory (`project_kernel_driver_family_subdirs`). | **1.7x** | confirm whether WiFi and BT split into separate cycles |
| **Intel platform** (templemount i9) — carries the **i225-V NIC** | A separate hardware line, not an AMD blocker. r8169/RTL8125 is done and iron-verified, so the **i225-V ethernet driver is the concrete first deliverable** and the reason this slot is now near rather than far. | **1.58.x** — operator call 2026-08-11 | — |
| **aarch64 / Pi platform** | Pi-class and other open-ARM SBCs (BeagleBone, ODROID, Rockchip). | **1.6x** | — |
| **RISC-V platform** | Boards on hand. Shifted from 1.7x when radios claimed that decade. | **1.8x** | — |
| **agnos 2.0 — clean refactor/rewrite** | The old "held on Cyrius's cadence" gate is largely cleared (type system v5.10.x, closures v6.x, userland PIE v6.1.41). The genuinely unshipped prerequisite is **native generics + monomorphization**, which cyrius carries *unpinned* in `roadmap-future.md`. | deferred | cyrius generics; and the base-to-server surface backlog above takes priority |

**Uncertain — verify on the next burn:** two carried from the 1.56.43 HID work
([card](https://github.com/MacCracken/agnosticos/blob/main/docs/development/iron-nuc-zen-log.md#tracker-15643-hid)),
both needing a boot **with the mouse attached** — the 2026-08-11 burn ran without one:
- **The mouse one-shot's deferred flush on iron.** QEMU-proven and mutation-tested; the keyboard half is
  iron-proven, the mouse half is not.
- **Whether the composite keyboard's phantom mouse interface is genuinely mute.** ⛔ Do NOT record the
  2026-08-11 burn as evidence either way: no mouse report appeared, but nothing in the log shows a media
  key was ever pressed, and a keypress leaves no trace unless it produces output. **A stimulus that may
  never have fired reports zero regardless of the truth.** Press Volume-Up and *say so* — the question is
  whether interface 1 emits in report protocol, which would let a media key move the cursor.

Not a burn question: whether `BENCHMARKS.md` and `bench-history.csv` are git-tracked (`git ls-files`).

---

*Built with cyrius 6.5.36 · `VERSION` is the single source of truth for the kernel version; the Current line
is maintained by `scripts/version-bump.sh`. At each arc close: delete the arc's forward-facing rows, let the
CHANGELOG carry the history, and keep this file under ~120 lines. Per-cut prose does not belong here.*
